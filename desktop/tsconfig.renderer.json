{
  // Renderer and the tests that drive it — these do need the DOM.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": []
  },
  "include": ["src/renderer/**/*.ts", "src/shared/**/*.ts", "test/**/*.ts"]
}
