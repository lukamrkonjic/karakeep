// Fork: the list of settings pages (SettingsPhoneNav, in the layout) — the
// phone's way in. The desktop's profile menu goes straight to /settings/info.
export default function SettingsHomepage() {
  return null;
}
