# Fork Changes & Merge Guide

This file catalogues every change this fork has made on top of upstream
[karakeep-app/karakeep](https://github.com/karakeep-app/karakeep), so that
pulling in upstream updates is easy to reason about and merge conflicts are
quick to resolve.

## TL;DR — how updatable is this fork?

**Very.** Of the ~42 changed files:

- **8 are brand-new files**, plus the whole of `desktop/`. Upstream will
  never touch these, so they can never conflict.
- **~20 are small, targeted edits** (a handful of lines each — removing a
  border class, dropping a prop, adding one line). If upstream changes a
  *different* part of the same file, git merges these automatically; if it
  touches the exact same line, the conflict is a one-glance fix.
- **~8 files were substantially reworked** and are the real conflict risk on
  a `git merge upstream/main`. They're called out explicitly below with
  guidance on how to reconcile them.
- **Eight migrations.** `0094_add_list_position.sql` adds a `position`
  column to `bookmarkLists` (list drag-to-reorder) and backfills it from
  `createdAt` for existing rows; `0095` is data-only (mangled list icons);
  `0096_list_subscriptions.sql` adds the two list-subscription tables and a
  user setting (Pinterest board sync — see below); `0097` adds
  `user.uiPreferences` (per-account UI preferences); `0098` adds
  `instagramSessions` (Instagram collection sync); `0099` adds
  `pictureEmbeddings`, `duplicatePictures` and `pictureDuplicateScans`
  (duplicate pictures); `0100` adds `listSubscriptions.wholeCarouselSince`
  ("Whole carousels"); `0101` adds the picture features' tables (settings,
  job runs, list suggestions, description cache) and two columns. A
  separate additive
  `AssetTypes` enum value (video thumbnails, `packages/db/schema.ts`) needed
  no migration at all — SQLite/Drizzle don't enforce enums at the DB level,
  so `drizzle-kit generate` reported no schema change for that one. Aside
  from these, this is overwhelmingly a cosmetic/UI fork plus a few targeted
  backend robustness/performance/feature additions.

## One-time setup (if not already done)

```bash
git remote add upstream https://github.com/karakeep-app/karakeep.git
git fetch upstream
```

## Recommended update workflow

1. `git fetch upstream`
2. `git checkout main && git merge upstream/main`
3. Resolve conflicts using the file-by-file guide below — check the 🔴
   section first, since that's where an actual conflict is most likely.
4. Re-run the full check before pushing:
   ```bash
   pnpm turbo --no-daemon typecheck lint format --continue
   pnpm --filter @karakeep/trpc test
   ```
   If the merge touched `packages/db/drizzle/`, also confirm the snapshot
   chain is intact — this must print *"No schema changes, nothing to
   migrate"*:
   ```bash
   pnpm --filter @karakeep/db exec drizzle-kit generate
   ```
5. Push → wait for **Build Fork Image** to go green → the NAS follows
   `:latest` and a DSM scheduled task downloads it within ten minutes, so
   `sudo docker compose up -d` in `/volume1/docker/karakeep` switches to it.
   `.claude/skills/update-karakeep/SKILL.md` has the one-time setup,
   rollback (pin a commit tag) and the fixes for errors seen so far.

### Note for checkouts on Windows

Git here runs with `core.autocrlf=true`, so every file lands in the working
tree with CRLF endings while `oxfmt` and the OpenAPI generator both emit LF.
That makes two of the checks above fail on *every* package — including ones
the merge never touched — for reasons that have nothing to do with the code:

- `format` (`oxfmt --check`) reports issues repo-wide.
- `pnpm run --filter @karakeep/open-api check` exits 1 with no message.

Neither is a real failure, and neither should be "fixed" by reformatting the
repo. To check them meaningfully, run the formatter/generator in write mode
and then look at `git diff` — git normalises line endings through the index,
so an empty diff means the content was already correct:

```bash
npx oxfmt <the files you changed> && git diff --stat -- <those files>
```

The same applies to the husky pre-commit hook, which runs the OpenAPI check;
`git commit --no-verify` is the pragmatic way through it on this machine.

---

## 🟢 New files (zero merge risk)

| File | Purpose |
|---|---|
| `desktop/` (whole directory) | **Magpie**, an Electron collector browser, plus the Karakeep Drop tray app it grew out of. Lives outside the pnpm workspace with its own `package.json` and lockfile — see `desktop/README.md` for why — so upstream never touches it and the server's Docker build never installs it. Talks to the server only through the public REST API, so it needs no backend change. |
| `.github/workflows/fork-build.yml` | Builds and pushes `ghcr.io/<you>/karakeep:latest` + `:<short-sha>` on every push to `main`, using the built-in `GITHUB_TOKEN` (no secrets needed). `SERVER_VERSION` is `fork-<short-sha>`, so Settings → Admin shows which build runs |
| `.claude/skills/update-karakeep/SKILL.md` | The NAS update guide, as a Claude Code skill (`/update-karakeep`): the NAS follows `:latest`, a DSM scheduled task pulls it every 10 minutes, and `docker compose up -d` switches to it; plus the one-time setup, stop/start, rollback by pinning a commit tag, and the errors seen so far (wrong folder, `compose.yaml` not `docker-compose.yml`, the dead `gcr.io` Chrome image). Upstream's own `skills/SKILL.md` is a different thing — how to use the Karakeep CLI |
| `.claude/skills/push-karakeep/SKILL.md` | Committing and pushing to `main`, as a Claude Code skill (`/push-karakeep`): the checks the husky hook would run (it always fails on this Windows checkout: CRLF), then `git commit --no-verify` without asking, the push, and waiting for the image build |
| `apps/web/components/dashboard/bookmarks/BookmarkVideo.tsx` | Video attachments. In the feed the tile is its poster (no network activity), a play mark shows on hover, and a click opens the preview modal; while the pointer is on the card (the card's whole box, found by `data-bookmark-index`, since the title and buttons sit on top) one player is mounted and plays — Settings → Videos turns that off, or on with sound (browsers only allow sound after a click on the page, so it falls back to muted until then). In the modal it autoplays — only the copy actually on screen, since the preview renders its wide and narrow layouts side by side and a CSS-hidden `<video autoplay>` still plays (doubled audio) |
| `apps/web/components/dashboard/bookmarks/MasonryMediaCard.tsx` | Eagle/Pinterest-style borderless, media-only masonry tile with hover-dim + white action icons; the hover title stays on one line (the full title is its tooltip; a note carrying a video is named by the video's file), and image tiles get the `ImagePeek` magnifier (not in bulk edit); while a mouse rests on a tile, the lists it's in show as chips along its bottom (`HoverListChips`) |
| `apps/web/components/dashboard/bookmarks/HoverListChips.tsx` | The lists a picture is in, as small dark-glass chips along the bottom of its masonry tile while a mouse rests on it — clear of the title and actions (top) and of the magnifier (bottom right). Asked for only after 150ms on the tile (sweeping across the grid asks for nothing); a tile hovered before shows its chips at once from the cache and refreshes them. Three at most, else two and "+N" (its tooltip names the rest); a chip opens its list, its tooltip is the list's path; a drag that starts on a chip drags the picture. Not in select mode, not by touch |
| `apps/web/components/VranaMark.tsx` | **vrana's logo**: a crow ("vrana" is Croatian for crow, drawn on a 64 grid) on a rounded square in the theme's blue (`fill-primary`), the crow in the page's background colour — in the light theme exactly the app icon (`public/icons/`, `app/apple-icon.png`, `public/favicon.ico`: beige #fbfaf7 on klein blue #286ff0), in the dark a charcoal crow on the softer blue. `public/offline.html` inlines it. To redraw the icons, these are the paths |
| `apps/web/components/dashboard/bookmarks/NewBookmarkDialog.tsx` | The "+" button/dialog that replaced the inline "NEW ITEM" editor card |
| `apps/web/lib/assetLoadGate.ts` | Shared concurrency limiter (separate budgets for images vs. videos) preventing a scroll burst from overwhelming the server's connection pool |
| `apps/web/lib/hooks/useNearViewportLoadSlot.ts` | Hook combining an IntersectionObserver with the load gate above |
| `apps/web/lib/emoji.ts` | `isEmojiIcon()` — used to hide the `??` placeholder glyph when a list has no real emoji icon |
| `tools/seed-snapshot/src/seed-media.ts` | Dev-only helper: seeds placeholder image/video bookmarks into a local instance for visual testing (not used in production) |
| `apps/web/components/dashboard/bookmarks/GatedImage.tsx` | Shared `<Image>` wrapper that only mounts once granted a concurrency slot (extracted out of `MasonryMediaCard`; reused by `BookmarkVideo`'s thumbnail placeholder) |
| `apps/web/lib/bookmark-drag.ts` | HTML5 drag-and-drop MIME constants for dragging bookmarks onto a list — the value is the dragged ids, comma-separated (a selection drags all of it) — and `isBookmarkDrag` / `draggedBookmarkIds` / `isAddDrag` (Ctrl or Alt/Option held: add instead of move) |
| `apps/web/lib/hooks/useBookmarkDragStart.ts` | `useBookmarkDrag(bookmark)` → `{ draggable, onDragStart }`, spread on the whole card in every layout (not by touch, where a long press opens the card's actions). A card that's part of the selection drags the selection. Tags the drag with the source list ID only when dragged out of a manual list; allows `copyMove` (on Windows Ctrl forces "copy", which `move` alone would refuse); draws the drag image (`bookmarkDragImage.ts`) |
| `apps/web/lib/bookmarkDragImage.ts` | The drag image: the card's own loaded picture (or playing video) drawn on a canvas at the screen's pixel density — rounded, white-edged, shadowed, keeping its shape within 104px — with up to two cards stacked behind and a count badge when several are dragged; a card without a picture shows its title. A canvas because an <img> may not be decoded yet when `setDragImage` snapshots it |
| `apps/web/lib/hooks/useListDrop.ts` | A list as a drop target (the sidebar's rows, a list page's sub-list tiles): a drop MOVES — `addToList`, then `removeFromList` from the source list and its sub-lists, or, dragged from a view that isn't a list, from every manual list it's in (read fresh via `getListsOfBookmark`) — or, with Ctrl/Alt held, only adds. Several bookmarks at once. `over` ("move"/"add") for the target to show; `onHover` after 650ms (the sidebar unfolds a folded list, even one that can't take the drop). Existing tRPC mutations, no backend change. Also: `hint` — "Move 3" / "+ Add", the count from `draggedBookmarkCount()` set at dragstart; after a drop the message has an **Undo** that puts every bookmark back in the lists it left and out of the one it joined; a moved selection is let go, an added one stays picked for the next list |
| `apps/web/components/dashboard/preview/BookmarkListChips.tsx` | The preview's "Lists" section (Eagle's "Folders"; replaced the old title-row list badges): a chip per list the bookmark is in (its emoji or a folder icon, and an × — always shown — that removes it from that list), a + that adds it to another list, "Unsorted" when it's in none. This is the one place a bookmark goes into several lists — dragging onto a sidebar list moves it |
| `apps/web/components/dashboard/preview/MediaFitPreview.tsx` | Preview-modal layout for image/video bookmarks (and notes carrying a video): the dialog wraps the media at its own size, capped to the viewport, instead of a fixed 90% box; the details sit in a panel beside it exactly as tall as the media that scrolls on its own (absolutely positioned inside its column so it never adds height). `getPreviewMedia()` decides what counts as media |
| `apps/web/lib/selection.ts` (+ `selection.test.ts`) | **Selecting like a desktop.** `startSelection` (the round box, a long press, "…" → Select), `toggleSelection` (a click in select mode, Ctrl/⌘-click), `selectRangeTo` (Shift-click: everything from the last one picked, in the grid's order), `setSelection` (the box drag), `selectAllLoaded` (Ctrl/⌘+A, Select all) — your own bookmarks only. They write `useBulkActionsStore` directly and keep select mode on with nothing picked (upstream's `toggleBookmark` closed it on the last untick, which made the bar vanish mid-task); the range start lives here |
| `apps/web/components/dashboard/bookmarks/GridSelection.tsx` | Rendered by `BookmarksGrid`: a press on empty space around the cards (not a card, a control or text) and a drag draws a box in the theme's blue that picks every card it touches, live; the page scrolls near its top/bottom edge; Ctrl/⌘ or Shift held at the start adds to what's picked; Esc puts things back; a box that picked nothing when nothing was picked leaves select mode off. Mouse only. Also Ctrl/⌘+A (not in fields or dialogs) |
| `apps/web/lib/list-drag.ts` | HTML5 drag-and-drop MIME constant for reordering sidebar lists (distinct from `bookmark-drag.ts`, which is for dragging bookmarks *onto* a list) |
| `apps/web/components/dashboard/lists/ListSubfolders.tsx` | Eagle-style row of subfolder tiles shown at the top of a parent list's page, above its bookmarks |
| `packages/db/drizzle/0094_add_list_position.sql` | Real migration: adds `bookmarkLists.position` (real, not-null, default 0) + an index, then backfills existing rows from `createdAt` so they keep their creation order instead of all tying at 0. **Was `0086_` until the first upstream merge, which brought its own `0086`–`0093`.** When renumbering a fork migration that is already deployed, keep its original `when` in `meta/_journal.json` (here `1782916466061`) and rebuild its snapshot on top of the new predecessor: drizzle applies any migration whose journal `when` exceeds the newest `created_at` in `__drizzle_migrations`, so an unchanged timestamp is what stops a live database re-running an `ALTER TABLE` that would fail. Confirm with `drizzle-kit generate` — it must report *no schema changes*. |
| `apps/web/lib/sidebarCollapse.ts` | Whether the desktop sidebar is folded in — an account preference (`sidebarCollapsed`, see `uiPreferences.tsx`): `useSidebarCollapsed()`, `useToggleSidebarCollapsed()` |
| `apps/web/components/shared/sidebar/SidebarCollapseWrapper.tsx` | Wraps the sidebar `<aside>`; collapses its width to 0 (overflow-hidden, animated) instead of unmounting it, so its scroll position/state survives a fold in/out |
| `apps/web/components/shared/sidebar/SidebarCollapseToggle.tsx` | Small chevron beside the header logo that folds the sidebar in/out (replaced `KarakeepLogoToggle`, which made the logo itself the toggle — the logo is a link home again). The state is the account's and seeds the server render, so the first paint already has the right arrow |
| `apps/web/lib/tailoredFeed.ts` | Which lists the tailored feed draws from — an account preference (`tailoredFeedExcluded`), so every device shows the same feed. Stored as EXCLUSIONS, so a list made later is in the feed until you take it out |
| `apps/web/components/dashboard/feed/TailoredFeedSettings.tsx` | The feed's list picker ("…" → "Choose lists…", in the sidebar and on the page; controlled `open`/`setOpen`, trigger optional): the sidebar's own tree with a tick per list, reusing `CollapsibleBookmarkLists`. A folder ticks/unticks everything under it and shows half-ticked when only some of it is in; `feedCandidates()` is the shared "your own manual lists" rule |
| `apps/web/components/dashboard/feed/TailoredFeed.tsx`, `apps/web/app/dashboard/feed/page.tsx` | The page at /dashboard/feed: "N of M lists", its "…" menu (`TailoredFeedOptions`: Choose lists…, Sort), and one grid of everything in the chosen lists (`listIds`) |
| `apps/web/components/dashboard/bookmarks/ClientBookmarksGrid.tsx` | `UpdatableBookmarksGrid` for pages whose query only exists in the browser (the feed's list choice, the tag filter): fetches the first page client-side instead of on the server, and is keyed on the query so a new choice starts a fresh grid |
| `apps/web/components/dashboard/tags/TagFilterView.tsx`, `TagsHome.tsx` | The tags page: find things by tag first (type to narrow the cloud, click tags, everything carrying ALL of them shows below, selection in `?with=`), with upstream's `AllTagsView` management one click away |
| `apps/web/components/utils/useShowSublists.ts` | The "…" menu's "Show items from sub-lists" setting — an account preference (`sublists`) that the server-rendered list page reads too, so it builds the right query straight away instead of flashing the parent's own items first |
| `packages/db/drizzle/0095_clear_mangled_list_icons.sql` | Data migration: clears list icons that are only "?"/U+FFFD/blanks. 94 of Luka's 103 lists stored `??` — an emoji that crossed a non-Unicode code page, one "?" per UTF-16 half — and every client printed it in front of the list name (web, browser extension, API). Pairs with `normalizeListIcon()`, which stops new ones being written |
| `apps/workers/workers/subscriptionWorker.ts` | **List subscriptions** — keeps a manual list in sync with a public Pinterest board. An hourly cron queues what is due (per-user interval, 10 min slack so "every 3 h" doesn't drift to 4), the runner reads the whole board and files what is new: oldest first, each bookmark stamped with its own second (`createdAt` is second-precision and ties sort by random id), so the list shows the board's order. The `listSubscriptionImports` ledger means nothing downloads twice: a pin the subscription handled is never touched again (even after its bookmark was moved or deleted), and a picture the user already has — the same image pinned twice, or a board removed and added again — is linked into the list instead of copied. Downloads stream to disk under `MAX_ASSET_SIZE_MB`; a picture/video that can never be had is remembered as skipped, a passing failure retried next sync; five failures in a row, the quota, or losing the list stop the run with a message. Boards over 300 new pins continue in back-to-back runs. Worker name `subscription` (for `WORKERS_ENABLED_WORKERS`/`WORKERS_DISABLED_WORKERS`). **Whole carousels:** the ledger is read with when each item was taken, and `connectors/ledger.ts` decides what counts as new (the Instagram connector's early stop asks it too); the cron's due test is `isSubscriptionDue()` from `packages/shared/utils/listSubscriptions.ts`, which the list header's next-sync time uses as well. **Skip near-duplicates** (per subscription): a downloaded picture is fingerprinted and compared with the user's fingerprints (`workers/pictures/nearDuplicates.ts`); alike enough (Settings → Pictures → Near-duplicates on import: the duplicate levels) it's linked like an already-saved one and the file dropped — outcome `alike`, "N near-duplicates of saved ones" in the log; a picture let in gets its fingerprint straight away, so a later copy in the same sync is caught too. Videos aren't checked. A model that can't load turns the check off for that sync. A sync that downloaded something asks for list suggestions (which wait for fingerprints) |
| `apps/workers/workers/connectors/pinterest.ts` | Reads a public board: the page's `__PWS_INITIAL_PROPS__` for page 1, then `BoardFeedResource/get` with the page's cookies + app version + CSRF token **and `x-pinterest-pws-handler`** (without it: 403 "Invalid Resource Request"), `filter_section_pins: false` like the page itself. Takes only each pin's own media from `*.pinimg.com` — the `.mp4` of a video (its `video_list` puts two HLS playlists of the same width first, which can't be stored), else `images.orig` — so avatars, favicons and "more ideas" never come in; skips `story` modules. An **idea pin**'s video isn't in `videos` (null) but on its pages (`story_pin_data`), where the feed lists only its HLS playlist; its mp4s sit beside it (`/videos/iht/hls/…/<id>.m3u8` → `/videos/iht/expMp4/…/<id>_720w.mp4`, also 540w/360w), so those are tried before the cover (`pinterest.test.ts`). `image_signature` is the media key. If Pinterest changes its front-end, this file is the one to fix. **Later: page 1 comes from `BoardFeedResource/get` too** (no `bookmarks` option): the page's own copy of the first 25 pins is cut short — no `carousel_data`, no `story_pin_data`, so an idea pin among them came in as its cover — and is only the fallback now. `itemsOf(pin)` is the pin's own item, then the rest of a carousel (`carousel_data.carousel_slots` but the one at `cover_index`, which is the pin's own picture as another file; slots are sized up to 736 px, so the original is worked out — `/736x/…` → `/originals/…`, `.jpg` then `.png` — before the 736x copy) or of an idea pin (every other page's video, or picture: its `originals` answers 403 and the `1200x` copy is what comes), each `<pinId>_<n>`, `partOf` the pin, keyed by its file's hash. Checked live 2026-09-26 on Wayfair's boards (81 pins + 21 more pictures; 113 + 83) |
| `packages/trpc/routers/listSubscriptions.ts` | `list`/`listAll`/`create`/`update`/`delete`/`runNow`. Needs edit rights on the list; `create` normalises any Pinterest domain to `www.pinterest.com` and syncs straight away; resuming a paused one syncs too. Deleting keeps the pictures and the ledger. `wholeCarousel` in and out — the column is `wholeCarouselSince`: `create` sets it to now (the default) or null; `update` takes `enabled` and/or `wholeCarousel`, and ticking it while it's on keeps the date it has (`listSubscriptions.test.ts`); `skipNearDuplicates` likewise |
| `packages/shared/types/listSubscriptions.ts`, `packages/shared/utils/pinterest.ts` | Zod schemas + interval choices; `parsePinterestBoardUrl()` (board links only — not pins, profiles or their `_saved`/`_created` tabs, sections, or hosts that merely contain "pinterest"); `wholeCarousel` (default true at create); `skipNearDuplicates` (default false) |
| `packages/shared/utils/instagram.ts` (+ `.test.ts`) | **Instagram saved collections as list subscriptions.** `parseInstagramCollectionUrl()` (`/<user>/saved/<name>/<id>/`, and "All posts": `/saved/all-posts/` or `/saved/`), the one stored form of a link, a collection's name; `parseInstagramSession()` takes whatever was pasted — the `sessionid` value (encoded or decoded), `sessionid=…`, Firefox's `sessionid:"…"`, a whole Cookie header |
| `packages/shared-server/src/instagram.ts` (+ `.test.ts`) | Instagram's web API with a pasted browser session (there's no public API for saved collections): the headers instagram.com's own page (and gallery-dl) send — **including `Sec-Fetch-Dest/Mode/Site`: without them Instagram answers an API call with its 650 KB web app** — a redirect/401/`login_required`/checkpoint means the session is spent (`InstagramSessionError`), 429 or "wait a few minutes" is only for now, and a reply that isn't `status: ok` JSON is no answer. `checkInstagramSession()` checks a pasted session can read the saved posts and fetches the username |
| `packages/shared-server/src/secretBox.ts` (+ `.test.ts`) | `sealSecret`/`openSecret`: AES-256-GCM with a key derived (HKDF) from NEXTAUTH_SECRET, which the web app and the workers share. The Instagram session is sealed with it; change the secret and it must be pasted again |
| `packages/trpc/routers/instagram.ts` (+ `.test.ts`) | `status`/`connect`/`disconnect`. `connect` parses the paste, checks it with Instagram on the spot, seals it; the session never comes back out through the API |
| `packages/db/drizzle/0098_instagram_sessions.sql` | Real migration: `instagramSessions` (one per user: the sealed session, the Instagram user id and username, `ok`/`expired`, when last checked) |
| `apps/workers/workers/connectors/instagram.ts` (+ `.test.ts`), `connectors/types.ts` | Reads a collection (`/api/v1/feed/collection/<id>/posts/`, or `/feed/saved/posts/` for All posts), 12 posts a page, 3–6 s apart. Every picture/video of a post is its own item — a carousel's each slide (`<code>_<n>`, linking to `…/p/<code>/?img_index=n`), a reel's widest mp4 before its cover — named by the caption's first line (or `@user`), media key `ig:<id>`, Instagram's CDN only. **Past the first sync it stops at the first page holding nothing new** (saves arrive at the top), so a routine sync is one or two requests. `types.ts` holds what every connector hands the worker (moved out of `pinterest.ts`, which re-exports it). Tested live 2026-09-24 against a real 1,400-item collection: 245 imported, 75 of them carousel slides, before it was stopped (videos not yet seen for real). **Whole carousels:** a carousel's slides after the first (the first that has media) are `partOf` it, and `isKnown` takes the item; `types.ts` has `partOf` |
| `apps/web/components/settings/InstagramConnection.tsx` | Settings → List subscriptions → Instagram: status (connected as @user / signed out), a three-step "How to connect" right above the paste field (an **Open Instagram** link; F12 → Storage/Application → Cookies; double-click `sessionid`'s value and copy it; Safari's developer-features switch), the field itself (a password input), disconnect, and what the session is (as good as the password; kept encrypted; Instagram may ask you to confirm it's you) |
| `apps/web/components/InstagramExpiredBanner.tsx` | An orange strip across the top of the app (above the header, in `SidebarLayout`) once the Instagram session is `expired` — found by a sync or the workers' daily check — with a link to Settings → List subscriptions (and not shown on that page, which says it itself). Closing it stores that expiry (the session's `checkedAt`) in the account's `instagramExpiryDismissed` preference: it never comes back for it, only for a later session that expires too. Seeded with the server's answer, so it's there on the first paint |
| `apps/workers/workers/duplicatesWorker.ts` | **Duplicate pictures, the way Immich finds its duplicates.** **Later: split in two jobs.** Looking at the pictures is the fingerprints job's now (`workers/pictures/fingerprintsWorker.ts`, see below; the model code moved to `workers/pictures/clip.ts`, the vector maths to `packages/shared-server/src/pictureVectors.ts`); this job only compares the fingerprints made since the last check (`compared: false`) with all of them, nightly or when asked (Settings → Pictures), and a check first waits for the fingerprints job (`pictureDuplicateScans.status` "waiting"). What follows describes the original single job: Every night at 03:00 (container time) a job per user: Immich's own CLIP model (`immich-app/ViT-B-32__openai`, `visual/model.onnx`, pinned revision + sha256, 352 MB, **downloaded once into `DATA_DIR/models`** on the first run) looks at each picture bookmark not seen yet — a video by its `linkVideoThumbnail` first frame — through `onnxruntime-node` on the CPU (2 threads; ~60 ms a picture on a desktop, expect a few times that on the NAS), and each new embedding is compared with all of the user's (cosine distance, as Immich measures). Pairs within 0.10 are stored; the page picks the level. The model is loaded only while there is something to look at and freed after. Preprocessing is Immich's `preprocess_cfg.json` (shortest side 224, bicubic, centre crop, OpenAI mean/std) — **with sharp's `fastShrinkOnLoad: false`: the default shrink-on-load made a lossless copy of a WebP score up to 0.05**. Robust to being cut short (6 h job limit): what was looked at stays, `compared` marks what still needs comparing. Measured on Luka's gallery photos: resized/mirrored copies 0.01–0.03, recropped/recompressed ~0.05, the closest two *different* photos 0.104 |
| `packages/trpc/routers/duplicatePictures.ts` (+ `.test.ts`) | `status`, `list({level})` (open pairs at that distance grouped by a chain of pairs, newest first, 50 at a time), `check` (Check now), `keep` (the kept one joins the others' manual lists, takes their tags, a favourite, a title/note it lacks; archived only if all were; then the others are deleted through `Bookmark.delete()`), `keepAll` ("not duplicates": pairs `kept`, never offered again), `keepBest` (every group keeps its most pixels → biggest file → first saved). **Later:** `check` marks the check "waiting" and asks for fingerprints; their job queues the comparison |
| `packages/shared/types/duplicatePictures.ts`, `packages/shared/utils/duplicateGroups.ts` (+ `.test.ts`) | The three levels (Identical ≤ 0.02, Near-identical ≤ 0.06 — the default, Similar ≤ 0.10; Immich's own default 0.01 missed half the plain resizes), the page's shapes, `bestDuplicate()`, and pairs → groups (union-find); the status has "waiting" (for the fingerprints) |
| `apps/web/components/dashboard/cleanups/DuplicatePictures.tsx` | Cleanups → Duplicate pictures (above duplicate tags): when it last checked and how many pictures it has looked at, Check now, the level (`duplicatePicturesMatch` preference), and each group side by side — size, date, source, lists, "Largest" — with Keep this one / Not duplicates: keep all, and "Keep the largest of each" behind a confirmation. Shows "Waiting for the new pictures' fingerprints…" and says the schedule is in Settings → Pictures |
| `packages/db/drizzle/0099_duplicate_pictures.sql` | Real migration: `pictureEmbeddings` (per picture bookmark: the asset looked at, model, 512 float32s or null if unreadable, width/height, compared), `duplicatePictures` (the pair in id order, distance, open/kept) and `pictureDuplicateScans` (each user's last check) |
| `apps/workers/workers/pictures/fingerprintsWorker.ts`, `jobs.ts` | **Picture fingerprints, their own job** (worker `fingerprints`, queue `picture_fingerprints_queue`): the picture model's embedding of every picture (a video by its first frame) without one, stored in `pictureEmbeddings` with `compared`/`suggested` false and `createdAt` = when (the web app's index cache goes by it); a replaced file drops its duplicate pairs and open suggestions. Cron on the half hour: every hour, or at 2:30 for "nightly" (per user, Settings → Pictures; "only when I ask" never), only when something needs looking at. When done it **queues what waits for it**: a duplicate check marked "waiting", list suggestions marked "waiting" — or on their own when new fingerprints were made. Status per user in `pictureJobRuns` |
| `apps/workers/workers/pictures/clip.ts` (moved from `duplicates/clip.ts`), `models.ts` | The model: one downloader for any of its files (sha256-checked, into `DATA_DIR/models/immich-app__ViT-B-32__openai/<rev>/`, the picture half where it always was), the picture half as before, and the **text half** (`textual/model.onnx`, 254 MB, input `text` int32 [1,77], output `embedding` [1,512]) with its tokenizer. `models.ts` shares one copy of each half among every job in the process (fingerprints, subscriptions, search): loaded on first use, one use at a time (async-mutex), let go of after 2 min (picture) / 10 min (text) unused |
| `apps/workers/workers/pictures/tokenizer.ts` (+ `.test.ts`) | CLIP's byte-level BPE in TypeScript, reading `textual/tokenizer.json`: NFC, whitespace, lower case; CLIP's split; GPT-2's byte map; merges with `</w>`; start/end, cut to 77, padded with end (as Immich pads). **Checked against Hugging Face `tokenizers` on 462 descriptions (2026-09-26): all identical** but for `<|startoftext|>`/`<|endoftext|>` typed as text, which this drops (HF makes them real markers, ending the description early) |
| `apps/workers/workers/pictures/textWorker.ts` | **Search by description**, the workers' half (worker `pictureText`, queue `picture_text_queue`, polled every 300 ms): fills in a `pictureTextQueries` row with the description's fingerprint; keeps the 1,000 most recently searched |
| `apps/workers/workers/pictures/suggestionsWorker.ts`, `suggest.ts` (+ `.test.ts`) | **List suggestions** ("Belongs in…", worker `suggestions`, queue `picture_suggestions_queue`): for each picture with a fingerprint not yet looked at (`suggested: false`) and in scope (saved since suggestions were turned on — `pictureSettings.suggestionsSince` — the last 30 days, or all), its 15 closest matches (similarity ≥ 0.70) among pictures in a list it isn't in vote for their lists, weighted by similarity (`suggest.ts`); a list with enough of the vote and matches (sure 60%/4, likely 40%/3, hunch 25%/2) is suggested — not one the picture is in or inside of, and a list gives way to one of its own lists. Stored in `pictureListSuggestions`; added or dismissed ones are never suggested again |
| `apps/workers/workers/pictures/nearDuplicates.ts` | `NearDuplicateFinder`: the subscription worker's near-duplicate check (see `subscriptionWorker.ts`) |
| `packages/shared-server/src/pictureVectors.ts` (+ `.test.ts`; moved from the workers' `duplicates/pairs.ts`) | The vector maths, now shared by the workers and the web app: distance, alike pairs, buffers, and `buildPictureIndex()`/`rankPictures()` (one Float32Array per user, dot products in one pass) |
| `packages/shared-server/src/pictureModels.ts`, `pictureSettings.ts` | The model's files (id, revision, paths, sizes, sha256) and whether each half is downloaded, for the web app too; `normalizeDescription()` and `pictureTextQueryId()` (the cache key). `getPictureSettings()`: a user's settings, the row created with the defaults when missing |
| `packages/shared/types/pictures.ts` | The settings (and defaults), the levels — similar: close 0.15 / related 0.22 / loose 0.30 (distance); description: strict 0.26 / balanced 0.23 / loose 0.20 (text–picture similarity); suggestions as above — calibrated 2026-09-26 on 84 real pictures: a description of something in them scores 0.26–0.33 at the top, of something absent ≤ 0.215 — and the status/suggestion shapes |
| `packages/trpc/routers/pictures.ts` (+ `.test.ts`), `packages/trpc/lib/pictureIndex.ts` | `settings`/`updateSettings` (turning suggestions on moves `suggestionsSince` to now; widening the scope re-looks at pictures), `status` (each job's last run, fingerprinted of total, models downloaded), `fingerprintNow`, `suggestNow`, `prepareTextModel`, **`similar`** (ranked by the user's index, paged, as bookmarks), **`searchByDescription`** (asks the workers for the description's fingerprint through `pictureTextQueries` + the queue and waits up to 8 s, else `preparing`; the search's qualifiers — `list:`, `#tag`, `is:fav` — narrow it through `getBookmarkIdsFromMatcher`), `suggestionsFor`, `suggestionGroups`, `acceptSuggestions` (adds through `ManualList.addBookmark`), `dismissSuggestions`. Open suggestions leave out ones whose picture is in the list meanwhile. `pictureIndex.ts`: each user's fingerprints in memory, reloaded when their count or newest `createdAt` changes |
| `packages/db/drizzle/0101_picture_features.sql` | Real migration: `pictureSettings`, `pictureJobRuns`, `pictureListSuggestions`, `pictureTextQueries`, `listSubscriptions.skipNearDuplicates`, `pictureEmbeddings.suggested`; then a settings row for every user with `suggestionsSince` = now (suggestions are for pictures saved from then on) |
| `apps/web/components/settings/PictureSettings.tsx`, `apps/web/app/settings/pictures/page.tsx` | **Settings → Pictures**: Fingerprints (schedule, Fingerprint now, how many have one, the last run, the model), Similar pictures (on/off, how similar), Search by description (on/off, matches, the text model: Download now / Downloading…), List suggestions (on/off, how sure, for which pictures, Suggest now, how many to look at), Duplicate pictures (every night / only when I ask), Near-duplicates on import (the level). Each job's line polls while it's waiting or running |
| `apps/web/components/dashboard/pictures/` (`SimilarPictures.tsx`, `ListSuggestionChips.tsx`, `SimilarPicturesPage.tsx`, `PictureSearchResults.tsx`, `pictures.ts`), `apps/web/app/dashboard/similar/[bookmarkId]/page.tsx` | A picture's details: **Similar** (up to 9 thumbnails, each opening in the preview's place — `replace` — and "See all N" → `/dashboard/similar/<id>`, the masonry grid of all of them) and **Belongs in** (dashed chips under Lists: + adds, x dismisses). Search → Pictures' results (the query settles 400 ms before it's sent; "Getting the text model ready…" while the workers load it). `pictures.ts`: a bookmark's picture (a video's first frame) and the accept/dismiss mutations with what they refresh |
| `apps/web/components/dashboard/cleanups/ListSuggestions.tsx` | Cleanups → **List suggestions** (above duplicate pictures): the open ones by list, largest group first — Add all / Not this list per list, ✓/✗ per picture |
| `packages/db/drizzle/0096_list_subscriptions.sql` | Real migration: `listSubscriptions`, `listSubscriptionImports` (the ledger: per user, `subscriptionId` set null on delete, indexed on `bookmarkId` so deleting a bookmark doesn't scan it) and `user.subscriptionIntervalHours` (default 12) |
| `apps/web/components/dashboard/lists/ListSubscriptionsModal.tsx`, `ListSubscriptionStatus.tsx` | The list "…" → "Add subscription" modal (add a board link, pause/resume, remove, "Sync now") and the shared status line ("Syncing…" polls every 2 s; when a sync finishes the grid and counts refresh). The add form (ticked) and every subscription have a **Whole carousels** checkbox: a native one in the theme's accent colour, as the app has no shadcn checkbox. And **Skip near-duplicates** beside it, on the add form (unticked) and each subscription (`OptionCheckbox`, a title explaining each) |
| `apps/web/components/settings/ListSubscriptionSettings.tsx`, `apps/web/app/settings/list-subscriptions/page.tsx` | Settings → List subscriptions: the schedule (only when asked / 3 h / 6 h / 12 h / daily / weekly) and every subscription with its list |
| `apps/workers/workers/connectors/ledger.ts` (+ `.test.ts`) | **Whole carousels** — what a sync takes of what a source lists: never what the subscription handled; the rest of a carousel (an item `partOf` its first) only with `wholeCarouselSince` set, and only for a post whose first picture came in since then, or hasn't yet. A post taken before keeps what it has: its other pictures turning up later would land at the top of the list, away from it. A picture that failed for now is still retried |
| `packages/db/drizzle/0100_list_subscription_whole_carousels.sql` | Real migration: `listSubscriptions.wholeCarouselSince` (nullable; null = only the first picture). Existing Instagram subscriptions get their `createdAt` (they always took every slide), Pinterest ones stay null (they only ever took each pin's own picture): nothing changes until a box is ticked |
| `packages/shared/utils/listSubscriptions.ts` (+ `.test.ts`) | When subscriptions sync: `isSubscriptionDue()` (the hourly cron's test, with its 10 min slack) and `nextSubscriptionSyncAt()` (the first hour it is due; null at "only when I ask"). The worker schedules with the one and the list header shows the other, so they can't disagree |
| `apps/web/components/dashboard/lists/ListSubscriptionNote.tsx` | Under a list's name, after its privacy: "↻ Syncs from Pinterest" (or Instagram, both, "2 Pinterest boards"), "(paused)", "Syncing…". Hovering lists each subscription and when it syncs next ("in about 3 hours, at 14:00", worked out when the tooltip opens) with its last sync; clicking opens the subscriptions for editors. Polls while a sync runs and refreshes the grid when it ends. Nothing for lists without subscriptions |
| `packages/trpc/routers/listSubscriptions.test.ts` | Whole carousels by default and `false` when asked, turning it on counts from then (ticking it while on keeps the date), pausing and it leave each other alone, only your own subscriptions |
| `packages/trpc/models/bookmarkOrders.ts` | getBookmarks' fork orders: **random** (each item ranked by a hash of its id and `shuffleSeed`, so a new seed per page load reshuffles everything while one scroll stays put) and **addedToList** (when a bookmark joined the list; the latest across lists for sub-lists/the feed; falls back to the saved date). Reads the matching ids + keys in one query, orders them, pages by the last item's key (not an offset, so a drag-and-drop move mid-scroll neither repeats nor skips) and loads the page through `loadMulti` by id. The key rides in the usual cursor's `id` as JSON, so no client or REST type changes |
| `apps/web/lib/pageSort.ts`, `apps/web/lib/hooks/usePageSort.ts` | Each page's Sort choice — an account preference (`pageSorts`: page key → sort, only non-defaults, the newest 200 kept) — so server pages load sorted, on every device; a change is saved at once, then the page refreshes |
| `apps/web/components/dashboard/sort/SortSubmenu.tsx`, `sort/PageSortButton.tsx`, `PageOptions.tsx`, `feed/TailoredFeedOptions.tsx` | The "Sort: …" submenu (Newest / Oldest / Recently added / Random), the home feed's header sort button, and the "…" menu for pages that aren't lists (sidebar variant on hover, header variant) |
| `apps/web/lib/previewDetails.ts` | Whether a preview shows its details panel — one account-wide setting (`previewDetailsHidden`) for every preview |
| `apps/web/components/dashboard/preview/ZoomableImage.tsx` | The preview's picture: wheel/pinch zooms at the pointer (up to 8×), drag pans (clamped so the picture covers its box), − / + / reset bottom right, a click at normal size is `onClick` (the modal closes). Native non-passive wheel listener; the pane's buttons and their groups (`data-pane-controls`, since a disabled button lets a click through to what's behind it) are excluded from pointer capture and from the click |
| `packages/shared/types/uiPreferences.ts`, `packages/trpc/routers/uiPreferences.ts` (+ `.test.ts`), `packages/db/drizzle/0097_ui_preferences.sql` | **UI preferences per account** (`user.uiPreferences`, JSON): upstream's view options and language, the theme, the tailored feed's lists, every page's sort, sub-list toggles, the sidebar and details toggles, the video hover settings — everything that used to live in each browser's cookies and localStorage. Every key optional (unset = the app's default); `update` merges per key inside one transaction, so two devices changing different things don't undo each other; reading drops a value stored in an unknown shape instead of the lot. Also which Instagram expiry's strip was closed (`instagramExpiryDismissed`, see `InstagramExpiredBanner.tsx`) |
| `apps/web/lib/uiPreferences.tsx`, `uiPreferences.server.ts` | The browser's copy: a zustand store seeded by the root layout on every load (server render and first paint agree), `usePreference(key)` / `useUpdatePreferences()`. A change applies at once and is saved 400 ms later (a slider drag is one request; `{ immediate: true }` for changes a server page must see, awaited before `router.refresh()`), flushed when the tab is hidden. `getUiPreferences()` is the per-request server read; `withAccountPreferences()` lays the account's values over upstream's cookie |
| `apps/web/lib/legacyPreferences.tsx` | The first signed-in load of a browser copies what it had customised (the fork's localStorage/cookies, upstream's view-options cookie, the theme) into the account wherever the account has nothing — non-default values only, checked against a fresh read of the account — then clears the fork's copies. Upstream's cookie and the theme key stay: signed-out pages still use them |
| `apps/web/components/dashboard/bookmarks/ImagePeek.tsx` | The magnifier bottom right on an image tile: pointing at it (after 120 ms, so a pointer passing by doesn't flash pictures) shows the picture over a backdrop as dark as the preview's (`bg-black/95`) at its OWN size, capped like the preview (`max-h-[92vh] max-w-[95vw]`) — never blown up — portalled and `pointer-events-none` so the pointer stays on the magnifier; a click opens the preview. A bare white icon like the tile's "…" (no circle). Only on devices with hover (`[@media(hover:hover)]`) |
| `apps/web/components/dashboard/preview/BookmarkNameInput.tsx`, `BookmarkProperties.tsx`, `PreviewActions.tsx` | The details panel, **laid out like Eagle's inspector** (see `BookmarkPreview.tsx`): the name as an input pre-filled with the title or file name (a video note's: its video's), saved on Enter/blur, emptied = back to the automatic title; **Properties** as label/value rows — a Favourite star (where Eagle has Rating), Dimensions (read from the picture/video the preview loads, not stored), Size (asset bookmarks), Type (the file's extension; a video note is a video), a link's Author/Publisher/Published, Date added/modified (formatted after mount, like upstream's creation time, so SSR can't mismatch); the footer, under a divider: **Download** (the file the bookmark is, named after the bookmark), Archive, Delete — labelled, left-aligned, pinned to the panel's bottom. It replaced upstream's centred icon row (`ActionBar.tsx`, now unused, kept for clean merges): its Edit went (everything is edited in place; the tile's "…" still has Edit) and its star moved into Properties |
| `apps/web/components/settings/VideoSettings.tsx` | Settings → User Info → Videos: "Play videos on hover" (on by default) and "Sound while hovering" (Muted / With sound) |
| `apps/web/components/shared/mobile/MobileTabBar.tsx` | **The phone's navigation (sm and down): a tab bar at the bottom** — Home, Feed (tailored feed), Lists (a sheet with the sidebar's lists, `AllLists pages={false}`), Search, More (a sheet: favourites, tags, archive, highlights, cleanups, settings, admin, dark mode, sign out). Fixed, above the home indicator (`env(safe-area-inset-bottom)`); `SidebarLayout` pads the page so it never covers the last row. Replaced `MobileListsMenu.tsx` (deleted) and the row of unlabeled icons at the top |
| `apps/web/components/ui/bottom-sheet.tsx` | `BottomSheet` / `SheetItem`: a Radix dialog sliding up from the bottom, closed by a tap outside, Escape, or dragging its top down. The tab bar's sheets, a card's actions, the viewer's details and lists |
| `apps/web/components/dashboard/header/HeaderMiddle.tsx` | The header's middle: on a desktop the search bar and page actions; on a phone the logo and the actions — and on the search page the search bar across it (the tab bar has Search) |
| `apps/web/components/settings/SettingsPhoneNav.tsx` | `/settings` is now a list of the settings pages (the tab bar's More leads there; the desktop's profile menu goes straight to /settings/info), and every settings page starts with "‹ Settings" on a phone. Replaces the settings' sideways strip of unlabeled icons |
| `apps/web/components/dashboard/preview/PhoneViewer.tsx`, `TouchZoomImage.tsx` | **A bookmark on a phone, full screen.** A picture or video on black (a note, link or PDF as a page), top bar (close, title, source), bottom bar (favourite, lists, share — the picture itself, so iOS offers Save Image — details, delete); the details panel and the list chips open as sheets. `TouchZoomImage`: pinch and double-tap zoom, pan when zoomed, tap hides the bars, drag down closes, drag sideways goes to the next/previous bookmark of the page behind (`useBulkActionsStore.visibleBookmarks`, `router.replace` to the next preview; neighbours' pictures preloaded) |
| `apps/web/lib/hooks/useIsPhone.ts`, `useLongPress.ts`, `apps/web/lib/store/useCardSheetStore.ts` | `useIsPhone` (≤ 640px, the grid's own phone breakpoint) and `useIsTouch` (pointer: coarse), false on the server; `useLongPress` (by touch — scrolling cancels it — and, given a third argument, the left mouse button held 500ms — moving 5px, i.e. a drag, cancels it; the click that ends it is swallowed); which card's actions sheet is open |
| `apps/web/components/PwaSupport.tsx`, `apps/web/public/service-worker.js`, `public/offline.html`, `public/icons/logo-maskable-512.png` | **The installed app.** `PwaSupport` registers the service worker in production (https only) as `/service-worker.js?v=<SERVER_VERSION>` (not `sw.js`: the root .gitignore still ignores that name from an old next-pwa setup) — each release its own cache, the last one's deleted — and keeps `theme-color` in the app's own theme. The worker caches only `/_next/static/*` (hashed names, never stale) and the offline page, never bookmarks or API answers; a page load that can't reach the server shows `offline.html`. Tested: registers, controls, caches 49 static files and 0 API calls, and with the dev server stopped a navigation shows the offline page. The maskable icon is for Android's shaped icons (vrana's crow, full bleed, inside the 40% safe circle). The offline page shows the logo and says vrana, in the theme's colours |
| `apps/workers/imageFormats.ts` | AVIF and other formats. `imageForAnalysis()` hands OCR and AI tagging a PNG copy of anything they can't read (AVIF — Tesseract and the vision APIs don't take it); `convertForStorage()` turns a format no browser shows (TIFF) into a PNG before the subscription worker stores it. Uses `sharp` (now a workers dependency, same version the web app already ships). HEIC, JPEG XL and BMP can't be decoded by the bundled libvips, so they stay unsupported |

## 🟡 Modified upstream files — small, targeted edits (low conflict risk)

| File | What changed |
|---|---|
| `apps/web/app/dashboard/{archive,favourites,lists/[listId],tags/[tagId]}/page.tsx`, `feeds/[feedId]/page.tsx` | Removed `showDivider={true}` prop (content-area divider removed) |
| `apps/web/app/dashboard/layout.tsx` | The desktop sidebar has no top nav any more: the lists lead (under Home — see `AllLists.tsx`), and Archive is a footer item pinned below them (`footerItems`). Search is the header bar, Highlights is in the profile menu. The mobile menu keeps every destination (it has no header logo to go home by); its list icon opens `MobileListsMenu` instead of the old All Lists page |
| `apps/web/components/dashboard/ErrorFallback.tsx`, `bookmarks/NoBookmarksBanner.tsx` | Removed border; kept a `bg-muted/40` panel for definition |
| `apps/web/components/dashboard/GlobalActions.tsx` | Added `<NewBookmarkDialog />` to the top header's action icons |
| `apps/web/components/dashboard/bookmarks/BookmarkActionBar.tsx` | Added an optional `className` prop (lets the masonry hover overlay force icons white) and `showExpand` (media tiles pass `false`: clicking the tile already opens the preview, so the expand icon was redundant there; link/note cards keep it) |
| `apps/web/components/shared/sidebar/SidebarLayout.tsx`, `apps/web/components/dashboard/header/Header.tsx`, `apps/web/app/dashboard/layout.tsx`, `apps/web/app/settings/layout.tsx`, `apps/web/app/settings/page.tsx`, `apps/web/app/admin/layout.tsx`, `apps/web/components/dashboard/header/ProfileOptions.tsx` (phone) | The phone shell: `mobileSidebar` is rendered as it is (the tab bar is fixed) and the page gets a 16px gutter and room for the tab bar; the header is 56px on a phone with the logo, the page actions and (on search) the search bar, and no profile menu. The dashboard, settings and admin layouts pass `MobileTabBar` (settings and admin now fetch the lists for its sheet; admin keeps its icon strip under it). `/settings` no longer redirects (it's the list of pages); the profile menu's User Settings goes to /settings/info |
| `packages/shared/types/uiPreferences.ts` (mobileGridColumns), `apps/web/lib/userLocalSettings/bookmarksLayout.tsx`, `apps/web/components/dashboard/bookmarks/BookmarksGrid.tsx`, `BookmarksGridSkeleton.tsx`, `apps/web/components/dashboard/ViewOptions.tsx` | **Columns on a phone.** Phones were always 1 column; now they have their own setting, `mobileGridColumns` (1–4, default 2), used below sm by the masonry breakpoints and the live column count. The view menu's Columns slider sets the one for the screen it's on ("on a phone", max 4). An 8px gutter and gap on a phone |
| `apps/web/components/dashboard/bookmarks/BookmarksGrid.tsx` (selecting), `BookmarkLayoutAdaptingCard.tsx` (`BulkEditSelectionOverlay`, `HoverActionBar`, compact actions), `MasonryMediaCard.tsx`, `BookmarkOptions.tsx` (Select), `apps/web/lib/hooks/useBookmarkKeyboardNavigation.ts` (Esc) | **Selecting (desktop).** A grid item picks with a mouse: a long press starts select mode with the card picked (by touch it still opens the card's actions), Ctrl/⌘-click toggles a card, Shift-click takes the range (and doesn't select text); it renders `GridSelection`. The overlay: before selecting, your own cards show the round box top left on hover (the masonry title makes room; not by touch) and clicking it starts selecting; while selecting the whole card is the button (Shift = range) at `z-[35]`, under the header and the phone's tab bar (at z-50 its boxes drew over both). Upstream's hover pill loses its select circle and hides while selecting (it sat over the card to be clicked), the compact layout's actions go under the overlay then, and a masonry tile hides its title row. The card menu's Select uses `startSelection`. An Esc that closed a dialog or menu (`defaultPrevented`, Radix) no longer also ends select mode |
| `apps/web/components/dashboard/PageOptions.tsx` (`SelectMenuItem`, `path`), `lists/ListOptions.tsx`, `tags/TagOptions.tsx`, `feed/TailoredFeedOptions.tsx`, `sidebar/AllLists.tsx`, `apps/web/app/dashboard/layout.tsx` | **"…" → Select**, first in the menu of the page you're on while it shows a grid of bookmarks: the page header's "…", and in the sidebar only the current page's row (rows pass their `path`; a list row compares its own). Starts select mode with nothing picked |
| `apps/web/components/dashboard/bookmarks/BookmarkOptions.tsx`, `BookmarksGrid.tsx` (long press), `BookmarkLayoutAdaptingCard.tsx` (selection), `BulkBookmarksAction.tsx`, `MasonryMediaCard.tsx`, `BookmarkVideo.tsx` | **Cards by touch.** A long press on a card opens its actions (the "…" menu's items as a sheet, sub-menus flattened; the "…" opens it too on a phone), with no iOS link callout; Select starts choosing several. In select mode every card shows a round box, ticked and ringed when chosen (it was a 10% tint). The bulk bar sits above the tab bar. Masonry tiles aren't draggable by touch (iOS would start a drag on the long press). A video tile's play mark always shows without hover |
| `apps/web/components/dashboard/preview/BookmarkPreview.tsx`, `apps/web/app/dashboard/@modal/(.)preview/[bookmarkId]/page.tsx` (phone) | A `"phone"` variant (`PhoneViewer`): the preview modal uses it on a phone (Radix dialog primitives filling the screen, not the dialog box), and a preview loaded as a page does too (closing goes Home) |
| `apps/web/components/dashboard/bookmarks/EditorCard.tsx`, `apps/web/components/dashboard/sidebar/AllLists.tsx` (pages), `apps/web/components/settings/InstagramConnection.tsx`, `ImportExport.tsx`, `apps/web/components/admin/BackgroundJobs.tsx`, `apps/web/components/dashboard/cleanups/DuplicatePictures.tsx` (phone) | New item has a **Photos** button (pictures and videos from the device, several at once; uploaded like a pasted image, into the current list), and no keyboard hint by touch. `AllLists` takes `pages={false}`. Instagram says to copy the cookie on a computer. Import cards and job buttons wrap instead of running off the screen; Cleanups' level buttons stay on one line |
| `apps/web/app/layout.tsx`, `apps/web/app/manifest.ts` | The installed app: `viewport-fit=cover`, `theme-color` light #fbfaf7 / dark #191715 (the theme's background), `apple-mobile-web-app-status-bar-style: default` (always readable: white status text over the light header would not be), `PwaSupport`. Manifest: `start_url` /dashboard/bookmarks with `id` "/" (an app installed from "/" stays the same app), `orientation: any`, a maskable icon, `name`/`short_name` "vrana", colours #fbfaf7, and no install `screenshots` (upstream's showed Karakeep's old look; `public/screenshots/` is left as it is) |
| `apps/web/app/layout.tsx`, `app/manifest.ts`, `app/public/lists/[listId]/page.tsx`, every page's `metadata` title (`… \| vrana`: `app/admin/*` ×4, `app/dashboard/*` ×7, `app/settings/*` ×14), `components/signin/SignInForm.tsx`, `signin/CredentialsForm.tsx`, `signup/SignUpForm.tsx`, `invite/InviteAcceptForm.tsx`, `admin/CreateInviteDialog.tsx`, `shared/sidebar/SidebarVersion.tsx`, `wrapped/ShareButton.tsx`, `wrapped/WrappedContent.tsx`, `lib/i18n/locales/en/translation.json` (3 strings) | **Renamed to vrana** — lowercase, even at the start of a sentence — wherever the app names itself: page titles, the manifest, the sign-in/up and invite texts, the sidebar's version label, Wrapped, three English strings. The code, packages, repo, image and API keep the name Karakeep, and so do "JSON (Karakeep format)" and "Import Bookmarks from Karakeep export" (the file format's name); other languages still say Karakeep. The sign-in page's "Or" separator and the "Sign up"/"Sign in" links take the theme's colours (they were hard-coded white, gray and blue). After a merge, `grep -rn Karakeep apps/web/app apps/web/components` finds upstream's new ones |

| `apps/web/components/dashboard/feed/TailoredFeed.tsx`, `apps/web/app/dashboard/feed/page.tsx` (hydration) | **Hydration fix:** the page passes the server's lists (`initialLists`); before, the server rendered a spinner while the browser (which had the sidebar's lists) rendered the feed — "Hydration failed" on every load of /dashboard/feed |
| `apps/web/components/dashboard/bookmarks/BookmarkLayoutAdaptingCard.tsx` (drag), `MasonryMediaCard.tsx` (drag), `BookmarkOptions.tsx` (Select), `apps/web/components/shared/sidebar/SidebarItem.tsx` (`dropHint`), `apps/web/components/dashboard/lists/ListSubfolders.tsx` (drop) | **Drag and drop, everywhere.** The whole card drags in every layout (`useBookmarkDrag`; the grip handle `DragHandle` is gone) and in select mode (dragging a selected card takes the selection); a list page's sub-list tiles take drops like the sidebar (`SubfolderTile` + `useListDrop`: ring, a small lift, Move / + Add); `SidebarItem` shows the `dropHint` pill while highlighted. The card menu's Select is on desktops too (a masonry tile had no other way to start a selection) |
| `apps/web/components/dashboard/bookmarks/BookmarkLayoutAdaptingCard.tsx` | Exported the bulk-selection overlay so `MasonryMediaCard` can reuse it. Upstream has since renamed it `MultiBookmarkSelector` → `BulkEditSelectionOverlay`; the fork took upstream's name and kept only the `export` |
| `apps/web/components/dashboard/header/Header.tsx` | `shrink-0` (a flex item in `SidebarLayout`'s column now); removed bottom divider; 64px→80px tall; search bar/profile icon padding aligned to match the grid's own inset; the logo is a `<Link>` home, with `<SidebarCollapseToggle>` beside it (see above) |
| `apps/web/components/dashboard/lists/ListHeader.tsx` | Added `<NewBookmarkDialog />` next to the `⋯` menu; icon hidden unless it's a real emoji; renders `<ListSubscriptionNote>` after the privacy label |
| `apps/web/components/shared/sidebar/Sidebar.tsx`, `SidebarLayout.tsx` | Removed the sidebar/content divider; `64px`→`80px` height calc (must match Header's new height everywhere it appears); top padding `16px`→`20px`; kept upstream's `xl:w-72`. Note the expanded width is duplicated in three places that must stay in sync — `Sidebar.tsx`, `SidebarCollapseWrapper.tsx` (which clips the aside, so a wider aside needs a wider wrapper) and `Header.tsx`'s logo block (`xl:w-[17rem]`). `SidebarLayout.tsx` also swapped its plain `<div>{sidebar}</div>` for `<SidebarCollapseWrapper>{sidebar}</SidebarCollapseWrapper>` (see 🟢 above). **Later: no height calc any more.** On desktop the page is a flex column — the Instagram-expired strip, the header, then the rest taking what's left (`sm:flex-1 sm:min-h-0`) — and the aside is `h-full`, so a strip above the header pushes nothing off the bottom. `SidebarLayout` is async now: it asks `api.instagram.status()` for the strip's first paint |
| `apps/web/components/ui/calendar.tsx`, `command.tsx`, `input.tsx`, `select.tsx`, `switch.tsx`, `tabs.tsx` | Flat-design pass: removed border/shadow, added `bg-muted` where needed for definition. (`button-group.tsx` and `input-group.tsx` also got this pass but upstream has since deleted both — nothing imported them — and the fork accepted the deletion.) |
| `apps/web/components/ui/card.tsx` | Removed border + shadow; background changed `bg-card`→`bg-muted` (bg-card is identical to the page background in light theme, so it was invisible) |
| `packages/shared-react/components/ui/textarea.tsx` | Removed border; `bg-background`→`bg-muted` |
| `apps/web/components/dashboard/preview/BookmarkPreview.tsx` | New `variant` prop: `"modal"` wraps a picture/video with `MediaFitPreview` and gives everything else an explicit 90vw×90vh box; `"page"` (default) fills its parent as before. `<BookmarkListChips>` is a section above Tags (the title-row badges are gone), and a video note's text shows in the details panel. **The details section is laid out like Eagle's inspector:** name input, Description (the note, above the tags), the tag editor, the source URL as a field with a globe (was a "View original" link, then a Source section), the AI summary (links), Lists, Properties, Attachments, Highlights, and the footer (Download/Archive/Delete) — no dividers, no all-caps headings. Upstream's `BookmarkMetadata`/`CreationTime`/`PublishedDate` went into `BookmarkProperties`; a video note's text shows small and muted, as the video's caption. `onClose` goes on to `MediaFitPreview`. The original wide layout's JSX is re-indented inside the `media ?` ternary, so a conflict there is usually indentation only |
| `apps/web/app/dashboard/@modal/(.)preview/[bookmarkId]/page.tsx` | `DialogContent` is sized by its content (`w-auto max-w-[95vw]`, no fixed height) and passes `variant="modal"` |
| `apps/web/components/dashboard/header/ProfileOptions.tsx` | Tags and Highlights entries at the top of the profile menu (moved out of the sidebar) |
| `apps/web/components/dashboard/preview/AssetContentSection.tsx`, `TextContentSection.tsx` | Their `<BookmarkVideo>` autoplays and gets the extracted poster frame |
| `apps/web/components/dashboard/bookmarks/AssetCard.tsx`, `TextCard.tsx`, `MasonryMediaCard.tsx` (video tiles) | Pass `bookmarkId` to `<BookmarkVideo thumbnail>` so the tile links to the preview |
| `packages/shared/types/bookmarks.ts` (getBookmarks input) | Added `listIds` (bookmarks in ANY of them) and `tagIds` (bookmarks carrying ALL of them). Internal only — this schema isn't part of the OpenAPI spec, so the REST surface is unchanged |
| `packages/trpc/models/bookmarks.ts` (loadMulti) | Both filters as subquery conditions on the plain-bookmarks path (which already restricts to your own bookmarks). Empty `listIds` returns nothing rather than everything, and neither combines with `listId`/`tagId`/`rssFeedId` — those drive their own query strategies. Covered by a test in `bookmarks.test.ts` |
| `packages/shared/utils/listUtils.ts` | `normalizeListIcon()` (the icon rule: an emoji or nothing) and `listNameFromPath()` no longer prints a leading space for a list with no icon |
| `packages/trpc/models/lists.ts` | Normalises the icon on create and update, so a mangled one can never be stored again (an edit that doesn't touch the icon still leaves it alone) |
| `apps/web/components/dashboard/lists/EditListModal.tsx` (icon default) | A new list starts with NO emoji (was `📁`, which is why lists seemed to need one); picking one still works |
| `apps/web/components/dashboard/sidebar/AllLists.tsx` (entries) | **The pages come first:** Home (`/dashboard/bookmarks`, same as the logo; it replaced "All Lists" and carries its list-invitation badge), Tailored feed, Tags, Favourites; then a gap and the **LISTS heading right above the lists** — only the lists scroll (the pages and heading stay put; the lists' scrollbar is always there, invisible until hovered, and the parts above reserve the same gutter with `scrollbar-gutter: stable`, so unfolding never narrows the rows) — with a Collapse all / Expand all toggle (its icon shows what a click does: collapse while anything is unfolded) beside the New list +. Which lists are unfolded is an account preference (`sidebarOpenLists`, through `CollapsibleBookmarkLists`'s `openState`); opening a list unfolds the way to it once per visit (upstream opened it on every render, so it could never be folded while open). Home, Tailored feed, Tags and Favourites each have a "…" on hover (`PageOptions`; Home's sorts the home feed); Home, Tailored feed, Tags and Favourites each have a "…" on hover (`PageOptions`; Home's sorts the home feed), and every row's "…" — lists' too — is right-aligned in the count's slot so they line up. Its `useDropTarget` now leaves the whole source folder: a bookmark dragged off a parent that is showing its sub-lists' items leaves the sub-list it actually sits in |
| `apps/web/components/dashboard/header/ProfileOptions.tsx` | Tags went back to the sidebar; Highlights stays in the menu |
| `apps/web/app/dashboard/lists/page.tsx` | **Redirects to Home.** The All Lists page is gone: everything on it is in the sidebar (on a phone, `MobileListsMenu`), and pending list invitations show on Home. Old links land on Home |
| `apps/web/app/dashboard/lists/[listId]/page.tsx` | Reads the sub-lists preference (`getUiPreferences()`) and asks for the list plus everything nested under it (`listIds`) when it's on |
| `apps/web/components/dashboard/lists/ListOptions.tsx` | "Show items from sub-lists", beside "Show Archived", on manual lists that have children |
| `apps/web/components/ui/dialog.tsx`, `app/dashboard/@modal/(.)preview/[bookmarkId]/page.tsx` | `DialogContent` takes an `overlayClassName`; the media preview dims the page to `bg-black/95`, and so does every other dialog now (the default overlay went `/80` → `/95`, to match the preview and the image peek). Dialogs have no border (it drew a light line round the preview) |
| `app/dashboard/{archive,favourites}/page.tsx`, `components/dashboard/lists/AllListsView.tsx` | Dropped the hard-coded 🗄️/⭐️/📋 from the page headers and the Favourites/Archive rows (`icon` is optional now). Lists you make keep whatever emoji you give them. (`AllListsView` is unused since the All Lists page redirects home; it's kept so upstream edits to it merge without a delete conflict) |
| `apps/web/components/dashboard/bookmarks/ManageListsModal.tsx`, `lists/DeleteListConfirmationDialog.tsx` | Build list labels through `listNameFromPath()` / skip the empty icon, instead of always `icon + " " + name` |
| `apps/web/components/dashboard/lists/EditListModal.tsx` | Added an X button to clear a list's emoji icon (shows a `Smile` placeholder when empty) |
| `packages/trpc/routers/lists.ts` (icon-clear) | `updateList` accepts `icon: null` to clear a list's icon — separate from the `stats` rewrite noted below |
| `packages/db/schema.ts` | Added `LINK_VIDEO_THUMBNAIL = "linkVideoThumbnail"` to the `AssetTypes` enum (TS-level only; no DB CHECK constraint, no migration generated). Also added `bookmarkLists.position` (real migration, see below) |
| `packages/shared/types/lists.ts` | Added `position: z.number()` to `zBookmarkListSchema` |
| `packages/shared-react/hooks/lists.ts` | Added `useReorderBookmarkList()`, mirroring the existing mutation-hook pattern (invalidates `lists.list` on success) |
| `apps/web/app/dashboard/lists/[listId]/page.tsx` | Renders `<ListSubfolders listId={list.id} />` under `<ListHeader>` |
| `packages/shared/types/bookmarks.ts` | Added `"videoThumbnail"` to `zAssetTypesSchema` |
| `packages/open-api/karakeep-openapi-spec.json` | Regenerated (`pnpm --filter @karakeep/open-api generate`) whenever an exposed Zod schema changes (so far: `zAssetTypesSchema`'s `videoThumbnail`, `zBookmarkListSchema`'s `position`) — the pre-commit hook runs `check` and fails the commit if this file is stale, since it's derived from the Zod schemas, not hand-edited |
| `packages/trpc/lib/attachments.ts` | Added `videoThumbnail` to the 4 exhaustive asset-type maps; marked not user-attachable/detachable (system-generated only) |
| `packages/shared-server/src/queues.ts` | Added optional `assetId` to `zAssetPreprocessingRequestSchema`, so a job can target a specific asset instead of only the bookmark's primary asset |
| `apps/web/lib/attachments.tsx` | Added a `videoThumbnail` icon mapping (used only if it's ever shown in a generic asset list) |
| `apps/web/components/dashboard/preview/AttachmentBox.tsx` | Filters `videoThumbnail` assets out of the user-visible attachment list (system-generated, not user-manageable). **Restyled for the Eagle-style panel:** a plain heading (no collapsing, no icons in it), no type icons on the rows, and adding is a labelled "+ Add file" ("+ Add banner" on links without one) under the files. New `mainAssetId` prop: the file the bookmark IS (picture/video/PDF/a note's video) isn't listed — it isn't attached to itself, and the panel's Download has it — and such a bookmark offers no "+ Add file" (that's for articles and notes), so its section only shows when something else is attached; read-only with nothing attached, it renders nothing |
| `apps/web/components/dashboard/preview/HighlightsBox.tsx` | A plain heading like the panel's other sections (it used to collapse) |
| `apps/web/components/dashboard/preview/NoteEditor.tsx` | Optional `placeholder` (the panel calls the note "Description") and a border like the other fields |
| `apps/web/components/dashboard/bookmarks/TextCard.tsx` (thumbnail lookup) | Looks up the video's `videoThumbnail` asset and passes it to both the masonry and standard render paths |
| `apps/web/components/dashboard/bookmarks/MasonryMediaCard.tsx` (thumbnail prop) | `media.thumbnailAssetId` passed through to `BookmarkVideo` |
| `packages/trpc/routers/admin.ts` | Added `generateVideoThumbnails` mutation: finds every video asset without a sibling `linkVideoThumbnail` and enqueues a job for it. Matches `assetType === "linkVideo"` OR (`assetType === "bookmarkAsset"` AND `contentType` is a video type) — `linkVideo` alone is trusted since it's an explicit label set at attach time, independent of `contentType` (which can be missing/wrong for assets bulk-imported straight into the DB, bypassing the sniffing upload endpoint — an initial `contentType`-only version of this query silently missed those). Takes a `regenerate` flag: when true it first deletes every existing `linkVideoThumbnail` (row + file) so the worker's "already generated" guard doesn't short-circuit, then re-enqueues all videos — used to repair the all-black thumbnails the old first-frame extraction produced. Also added the `cancelQueuedJobs` mutation (calls the given queue's `cancelAllNonRunning()` from liteque — aborts an accidental bulk enqueue like a runaway "reprocess assets"). And widened `getBookmarkDebugInfo`'s output schema's `assetInfo.assetType` to include `"video"` (an `.output()` schema trpc validates at runtime, so it throws if not kept in sync) |
| `packages/trpc/routers/bookmarks.ts` | `createBookmark`'s `ASSET` case now enqueues the video-thumbnail job (with `assetId` set) instead of the default image/pdf preprocessing job when `assetType === "video"` — the default path only knows "image"/"pdf" and was throwing `"Unsupported bookmark type"` for every directly-uploaded video, silently failing the job (see the ASSET-bookmark video fix below) |
| `packages/db/schema.ts` (bookmarkAssets) | Widened `bookmarkAssets.assetType` from `["image", "pdf"]` to `["image", "pdf", "video"]` so a directly-uploaded/drag-dropped video file can become a bookmark's own content (previously only possible via a `link` bookmark's attached video). TS-level enum only, no migration (same as the `AssetTypes` note above) |
| `packages/shared/types/bookmarks.ts` (asset bookmark) | Same `"video"` addition to the 3 `assetType: z.enum(["image","pdf"])` occurrences (`zBookmarkedAssetSchema`, `zNewBookmarkRequestSchema`, `zPublicBookmarkSchema`) |
| `packages/shared/assetdb.ts` | Added `...VIDEO_ASSET_TYPES` to `SUPPORTED_BOOKMARK_ASSET_TYPES` — this was the actual bug: video files uploaded fine (already in `SUPPORTED_UPLOAD_ASSET_TYPES`) but were rejected with "Unsupported asset type" the moment you tried to turn that upload into a bookmark |
| `apps/web/components/dashboard/UploadDropzone.tsx` | Fixed a real client bug: it hardcoded `assetType: contentType === "application/pdf" ? "pdf" : "image"` — any non-PDF file, including videos, got mislabeled `"image"`. Now checks for `video/*` too. Updated the drop placeholder text. **Also (paste-to-save):** `useUploadAsset` now reads `useBookmarkListContext()` and adds the created bookmark to the current manual list — so drag-drop, editor paste, and the new global paste all land in the list you're viewing (was previously created loose, never added to the list). And the component registers a `document`-level `paste` listener: a clipboard image (a macOS `⌘⌃⇧4` / Windows `Win+Shift+S` screenshot snippet) pasted anywhere on a bookmarks/list page uploads as a bookmark, skipping pastes that land in a text field / the note editor (`isEditableTarget` guard) and demo mode. Restructured `useUploadAsset` to sequence upload → create → add-to-list (create was previously fired inside `useUpload`'s `onSuccess`, so the new bookmark id wasn't available to add to a list). |
| `apps/web/components/dashboard/bookmarks/AssetCard.tsx` | Added a `"video"` case (renders `<BookmarkVideo>`) to `AssetImage`'s switch — it has an exhaustive `never` check, so this was a real compile error once the enum widened, not just a missing feature. Also extended the masonry-layout branch to cover video the same way `TextCard.tsx` does for link bookmarks. |
| `apps/web/components/dashboard/preview/AssetContentSection.tsx` | Added a `VideoContentSection` (mirrors `TextContentSection`'s `<BookmarkVideo>` usage) so the full preview modal plays a directly-uploaded video instead of showing "Unsupported asset type" |
| `packages/trpc/models/bookmarks.ts` (getBannerImageUrl) | Added a `"video"` case (returns `null` — no poster-frame generation for this path) to another exhaustive `never`-checked switch that broke once the enum widened |
| `packages/sdk/src/karakeep-api.d.ts` | Hand-patched the 2 `assetType: "image" \| "pdf"` occurrences to include `"video"`, instead of a full `openapi-typescript` regenerate — the installed codegen version (7.8.0) reformats the entire ~6800-line file well beyond this change, so a full regenerate would bury the real diff in unrelated noise. Only re-run the generator wholesale if you're already touching this file for another reason. |
| `apps/web/components/admin/BackgroundJobs.tsx` | Added "Generate missing video thumbnails" + "Regenerate all video thumbnails" buttons to the Asset Preprocessing job card (Settings → Admin → Background Jobs), and a "Cancel queued jobs" button to every queue card that supports it (crawler, inference, indexing, embeddings, asset preprocessing, video), each calling the mutations above |
| `apps/workers/workers/videoWorker.ts` | After an auto-downloaded video (yt-dlp, e.g. embedded YouTube/X/Reddit videos) is saved, now also enqueues a thumbnail job — this path writes the asset straight to the DB and previously bypassed thumbnail generation entirely, unlike the manual-attach path |
| `packages/trpc/testUtils.ts` | The shared-server mock also stubs `queueSubscriptionSync` (creating a list subscription queues its first sync, and tests have no queue tables) and `queueDuplicatePicturesCheck`. *(Earlier, no longer a fork change:)* The fork added `AssetPreprocessingQueue` to the shared queue mock; upstream has since added its own, wired to an observable `testQueueMocks.assetPreprocessingEnqueue`. **Merge trap:** git happily auto-merges both into the same object literal, and the duplicate key silently shadows upstream's mock with an anonymous `vi.fn()`, so assertions against it fail for no visible reason. Keep upstream's, drop ours. Also stubs `requestPictureFingerprints`, `requestListSuggestions` and `PictureTextQueue` (the picture features). |
| `packages/trpc/routers/admin.test.ts` | Adds tests for `generateVideoThumbnails` and `cancelQueuedJobs`. Uses upstream's own `beforeEach` (it clears mocks and installs the search/vectorStore mocks its tests need) — `defaultBeforeEach` isn't required here, because `testUtils.ts`'s `vi.mock("@karakeep/shared-server")` is hoisted to that module's top level and so applies to every test file importing it |
| `packages/db/schema.ts` (Instagram) | `instagramSessionsTable` (see `0098`); `listSubscriptions.kind` also takes `"instagram"` (TS-level enum: no migration) |
| `apps/workers/network.ts` (+ `network.test.ts`) | `fetchWithProxy` honours `redirect: "manual"`: a redirect comes back to the caller instead of being followed (every other caller still gets redirects followed and each hop validated). **Without it an expired Instagram session is never noticed:** Instagram answers one with a 302 to its login page, and following that ends in "Too many redirects", which reads as a hiccup |
| `apps/workers/workers/subscriptionWorker.ts` (Instagram) | Reads by `kind`: an Instagram subscription opens the user's sealed session (missing, expired or unopenable → the sync fails with what to do) and hands the connector the ledger so it can stop early; an `InstagramSessionError` marks the session `expired` (Settings and the app's orange strip show it; only the first finding stamps `checkedAt`, which closing the strip remembers). **The hourly cron also checks, once a day, every `ok` session nothing has used for 24 h** (`checkInstagramSessions`: the same two requests as Connect, skipping users whose Instagram sync it just queued), so an expiry shows up even without syncs. Per kind: the run's cap (Pinterest 300, Instagram 500), the read timeout (5 / 20 min), and the download referer. The ledger is read before fetching now |
| `packages/trpc/routers/listSubscriptions.ts` (Instagram), `packages/shared/types/listSubscriptions.ts` | `create` takes a Pinterest board or an Instagram collection link (`sourceOf()`); a collection needs Instagram connected with an `ok` session. The kind enum and `zInstagramConnectionSchema` |
| `apps/web/components/dashboard/lists/ListSubscriptionsModal.tsx`, `apps/web/components/settings/ListSubscriptionSettings.tsx` (Instagram) | Take either kind of link, show each subscription's source (Pinterest/Instagram), and point to Settings when Instagram isn't connected; the settings page has the Instagram section |
| `packages/trpc/routers/_app.ts` (instagram), `packages/shared-server/src/index.ts` | Register the `instagram` router; export the secret box and the Instagram client |
| `packages/db/schema.ts` (list subscriptions) | `listSubscriptionsTable`, `listSubscriptionImportsTable` and `users.subscriptionIntervalHours` (see `0096` above); `listSubscriptions.wholeCarouselSince` (`0100`) |
| `packages/shared-server/src/queues.ts` (subscriptions) | `SubscriptionQueue` and `queueSubscriptionSync()`: marks the subscription "pending" and enqueues with idempotency key `subscription:<id>`, so a sync already queued or running absorbs another request |
| `packages/trpc/routers/_app.ts` | Registers the router as `listSubscriptions` (upstream's `subscriptions` is Stripe billing) |
| `packages/shared/types/users.ts`, `packages/trpc/models/users.ts`, `apps/web/lib/userSettings.tsx` | The `subscriptionIntervalHours` user setting (0–720, 0 = only on "Sync now"), read, written and defaulted like the others |
| `packages/trpc/routers/users.test.ts` | The settings test asserts the exact shape, so it expects `subscriptionIntervalHours` (and sets it) |
| `apps/workers/index.ts` | Registers the `subscription` worker and starts/stops its cron, like the feed worker's |
| `apps/web/app/settings/layout.tsx` | "List subscriptions" entry under "RSS Subscriptions"; "Pictures" after it |
| `apps/web/components/dashboard/lists/ListOptions.tsx` (subscriptions) | "Add subscription" in the "…" menu, on manual lists you can edit |
| `apps/web/components/dashboard/lists/ListOptions.tsx` (sort) | "Sort: …" submenu (`BookmarkSortSubmenu`, key `list:<id>`; Recently added on manual lists) between the actions and the view toggles (`AFTER_SORT`) |
| `packages/shared/types/bookmarks.ts` (getBookmarks sort) | `sortBy: "random" \| "addedToList"` and `shuffleSeed` on the getBookmarks input. Internal only — the REST/OpenAPI surface is unchanged (the cursor keeps its shape; see `bookmarkOrders.ts`) |
| `packages/trpc/models/bookmarks.ts` (loadMulti) | One early branch after smart lists become ids: `sortBy` hands over to `loadInForkOrder`, which loads the page back through `loadMulti` by id. Upstream's own paths are untouched |
| `packages/trpc/routers/bookmarks.test.ts` (orders) | Random covers everything once across pages, is stable per seed and survives a bookmark leaving mid-scroll; Recently added follows the join date on both the list and the sub-lists/feed paths |
| `apps/web/components/dashboard/bookmarks/Bookmarks.tsx`, `UpdatableBookmarksGrid.tsx`, `ClientBookmarksGrid.tsx` | Take the page's `sortKey`: the server grid reads its sort from the account's preferences (with a fresh shuffle seed per load) and loads the first page in that order; the client grid does the same from the seeded store, without waiting for hydration. `UpdatableBookmarksGrid` uses a passed `sort` and falls back to upstream's global toggle without one |
| `apps/web/app/dashboard/{bookmarks,favourites,archive,lists/[listId],tags/[tagId],feeds/[feedId]}/page.tsx`, `lists/page.tsx` | Pass their `sortKey`; Favourites, Archive and RSS feeds get a header "…" (`BookmarkPageOptions`). `bookmarks/page.tsx` (Home) also shows `PendingInvitationsCard` above the grid (it renders nothing without invitations) |
| `apps/web/components/dashboard/GlobalActions.tsx` | The header sort toggle only on search (upstream's, with relevance); on the home feed it's the page's own sort (`PageSortButton`); elsewhere the page's "…" has it |
| `apps/web/components/dashboard/tags/TagOptions.tsx`, `tags/TagFilterView.tsx` | Sort in the tag page's "…" (`tag:<id>`); the tags page's "…" and grid (`tags`) |
| `apps/web/components/shared/sidebar/TSidebarItem.ts`, `Sidebar.tsx`, `apps/web/app/dashboard/layout.tsx` | Sidebar items can carry a `right` slot; Archive's is its "…" |
| `apps/web/components/dashboard/preview/MediaFitPreview.tsx`, `BookmarkPreview.tsx` (details toggle) | Both previews' details toggle reads one account setting (`lib/previewDetails.ts`); pictures render through `ZoomableImage`, where a click on the picture closes the modal (videos keep clicks for their player) and an Open button, left of the details toggle, opens the original in a new tab |
| `apps/web/app/layout.tsx`, `apps/web/lib/providers.tsx` | The root layout reads the account's UI preferences, lays them over upstream's cookie settings (so `<html lang>` and the first render are right) and hands them to `UiPreferencesProvider` (inside the tRPC provider), with `ThemePreferenceSync`, `LegacyPreferencesImport` (signed in) and `PreferredLanguage` (switches i18next in an effect — `CustomI18nextProvider` switches during render, which makes every translated component update mid-render). `ThemeProvider`'s `defaultTheme` is the account's theme, so a new browser paints in it |
| `apps/web/lib/userLocalSettings/bookmarksLayout.tsx` | Upstream's view-option hooks read the account's preference first, then the cookie context |
| `apps/web/components/dashboard/ViewOptions.tsx` | Saves to the account instead of calling the cookie server actions (no `useOptimistic`: the store changes at once); the columns slider updates the grid while it's dragged (`onValueChange`), saved once the drag settles |
| `apps/web/components/settings/UserOptions.tsx` (language), `apps/web/lib/i18n/server.ts` | The language is saved to the account first, then upstream's cookie (for signed-out pages); server-side translations use the account's language |
| `apps/web/components/theme-provider.tsx` | `ThemePreferenceSync` applies the account's theme when it differs from the browser's; `useToggleTheme()` saves the choice |
| `packages/db/schema.ts` (uiPreferences), `packages/trpc/routers/_app.ts` (uiPreferences) | The `user.uiPreferences` column and the `uiPreferences` router |
| `apps/web/components/shared/sidebar/MobileSidebar.tsx` | An `extra` slot after the plain links (unused since the tab bar; the admin pages still use the strip itself, under the tab bar) |
| `apps/web/app/settings/info/page.tsx` | `<VideoSettings />` after the user options |
| `apps/web/components/dashboard/lists/DeleteListConfirmationDialog.tsx`, `LeaveListConfirmationDialog.tsx`, `ListHeader.tsx` | Deleting/leaving the list you're on (or it vanishing) goes Home instead of to the All Lists page |
| `packages/shared/assetdb.ts` (AVIF) | `IMAGE_AVIF` in `ASSET_TYPES` and `IMAGE_ASSET_TYPES`, so an AVIF uploads, becomes an image bookmark, and a direct AVIF link is stored as a picture. Browsers show it as it is (`AssetCard` renders assets `unoptimized`) |
| `apps/workers/workers/assetPreprocessingWorker.ts` (OCR), `apps/workers/workers/inference/tagging.ts` | Pass the image through `imageForAnalysis()` first (see `imageFormats.ts` above); JPEG/PNG/WebP/GIF go through untouched, as before |
| `apps/workers/package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `docker/Dockerfile` (duplicate pictures) | `onnxruntime-node` 1.30.0 for the workers. `allowBuilds: onnxruntime-node: false`: its postinstall fetches NVIDIA CUDA libraries on Linux x64, while the CPU runtime is in the package. The package carries every OS's runtime (~290 MB), so the Dockerfile deletes all but `linux/<TARGETARCH>` after `pnpm deploy` (~45 MB left) and fails the build if that one is missing (tested under dash in WSL). tsdown leaves it external (a dynamic import, loaded only when a check runs) |
| `packages/db/schema.ts` (duplicate pictures), `packages/shared-server/src/queues.ts` (duplicate pictures), `apps/workers/index.ts` (duplicates), `packages/trpc/routers/_app.ts` (duplicatePictures), `apps/web/app/dashboard/cleanups/page.tsx`, `packages/shared/types/uiPreferences.ts` (duplicatePicturesMatch) | The three tables (+ the `blob` import); `DuplicatePicturesQueue` and `queueDuplicatePicturesCheck()` (marks the user's check pending unless one is running, idempotency key per user); the `duplicates` worker and its nightly cron, started/stopped like the subscription worker's; the `duplicatePictures` router; the Cleanups section; the level preference. **Later (picture features):** the four new tables and two columns (`0101`) and "waiting" in `pictureDuplicateScans`; the queues `PictureFingerprintsQueue`, `PictureSuggestionsQueue`, `PictureTextQueue` with `requestPictureFingerprints()`, `requestListSuggestions()`/`queueListSuggestions()`, and `queueDuplicatePicturesCheck()` now marking the check "waiting" and asking for fingerprints (`queueDuplicatePicturesCompare()` queues it); the `fingerprints`, `suggestions` and `pictureText` workers (the first with its cron); the `pictures` router; Cleanups' List suggestions section; shared-server exports its picture modules |
| `apps/workers/package.json`, `pnpm-lock.yaml` | `sharp` for the workers (the lockfile gains one importer entry; the package itself was already there for the web app). tsdown leaves it external and `pnpm deploy` ships its Linux binary |
| `desktop/src/main/ingest.ts` (Magpie) | AVIF in its copy of the accepted types, and sniffed as `ftypavif`/`ftypavis` before the generic `ftyp` → MP4 rule. Vrana (separate repo) has the same list and still lacks AVIF |
| `apps/web/components/dashboard/search/SearchModeSelector.tsx`, `SearchInput.tsx`, `apps/web/lib/hooks/bookmark-search.ts`, `apps/web/app/dashboard/search/page.tsx` | **Search → Pictures** (search by description): a `pictures` mode of the web app's own (`SearchMode` = upstream's modes + `"pictures"`, `?mode=pictures`), offered when Settings → Pictures has it on — the mode button shows for it even without upstream's semantic search, whose modes it keeps behind `semanticSearchEnabled`; its own placeholder; the page renders `PictureSearchResults` for it instead of `searchBookmarks` (upstream's search never sees the mode) |
| `apps/web/components/dashboard/preview/BookmarkPreview.tsx` (pictures) | The details show `ListSuggestionChips` under the Lists (owner only, in one block with them) and `SimilarPictures` after Properties (pictures and videos) |

## 🔴 Substantially reworked files — highest conflict risk, check these first

| File | What changed & why | Merge guidance |
|---|---|---|
| `tooling/tailwind/globals.css` | **Entire color palette replaced — vrana's.** Light: warm paper beige surfaces (#fbfaf7 for background, cards and popovers alike — never white), a klein blue accent (#286ff0) with white on it, ink, greys and hairlines tinted to the beige. Dark: warm charcoal surfaces that get lighter as they rise (card over background), the beige dimmed as the text, the blue lighter and less saturated (219 64% 66%) with dark ink on it, a softer red. Every CSS variable under `:root` and `.dark` was rewritten. The backgrounds are also written out in `PwaSupport.tsx`, `app/layout.tsx` (theme-color), `app/manifest.ts` and `public/offline.html`: change them together | If upstream adds/renames a variable, take upstream's *variable name*, but keep **our** color *value*. If upstream only tweaks values we didn't touch, this should merge cleanly. |
| `apps/web/components/KarakeepIcon.tsx` | Upstream's SVG logo (`public/icons/karakeep-full.svg`, now unused) replaced by vrana's: the small blue square (`VranaMark`), as tall as the type (1em), then the word "vrana"; the type is 0.64 × the `height` prop (24px in the header). Kept under its upstream name so the eight places that show it stay untouched | If upstream changes its logo component, keep ours. |
| `apps/web/public/icons/logo-{16,48,128,192,512}.png`, `logo-icon.svg`, `logo-icon-dark.svg` (the same crow in both schemes), `apps/web/app/apple-icon.png` (180px, full bleed — iOS rounds it), `apps/web/public/favicon.ico` (16/32/48) | Upstream's icons replaced by vrana's crow: beige on klein blue, a rounded tile (full bleed where the platform shapes the icon itself) | Binary: on a conflict keep ours (`git checkout --ours <file>`). |
| `apps/web/components/dashboard/BulkBookmarksAction.tsx` | **The bar is a card** (the render and its action list rewritten): one line in the look of the app's menus (`bg-popover`, hairline border, soft shadow) — ✕ (Esc too), the count as a chip in the theme's blue ("Click items, or drag a box around them" with none), Select all / Deselect all (yours only), then "Add to", "Remove from" (manual lists you can edit), Delete, and "…" with tags, favourite, archive, copy links, refresh, offline copy. Delete's dialog shows the pictures (read off the cards on screen, `pictureUrlOf`) and "This can't be undone". On a phone it sits above the tab bar; Delete and Select all go icon-only / into "…" | Keep ours; re-check upstream's new bulk actions and add them to the "…" list. |
| `apps/web/components/dashboard/bookmarks/TextCard.tsx` | Added a masonry-layout branch: renders `MasonryMediaCard` for video-attached text bookmarks instead of the standard card body. | If upstream changes the surrounding card logic, keep our added `layout === "masonry"` branch and reapply it around upstream's new code. |
| `apps/web/components/dashboard/bookmarks/AssetCard.tsx` | Same masonry-layout branch, for image assets. | Same approach as TextCard.tsx above. |
| `apps/web/components/dashboard/bookmarks/BookmarksGrid.tsx` | Removed the inline `EditorCard` from the grid entirely (replaced by the "+" dialog); masonry-layout cards get no border/`bg-card`; widened the `Masonry` gap 16px→20px; widened the infinite-scroll `rootMargin` so pagination fires ~1200px early. | Diff carefully — this file has four independent changes bundled in. Reapply each piece individually against upstream's version rather than doing a blind merge. |
| `apps/web/components/dashboard/bookmarks/EditorCard.tsx` | Added `inDialog`/`onCreated` props so the same component can render inside `NewBookmarkDialog` without its own card chrome (title row, fixed height). | Should merge cleanly unless upstream changes the same prop surface; if so, keep our two new optional props. |
| `apps/web/components/dashboard/lists/AllListsView.tsx` | *(Unused since the All Lists page redirects home.)* Removed the colored accent bar and the emoji's card/border/shadow from list rows; chevron space is now only reserved for rows that actually have subfolders (was previously reserved for every row). | Re-verify the `collapsible` conditional still gates the chevron `<div>` correctly after merging. |
| `apps/web/components/dashboard/sidebar/AllLists.tsx` | Same chevron-reservation fix, for the sidebar tree; removed the default 📋/⭐ emoji icons on "All Lists"/"Favourites"; each list row is a drop target through `useListDrop` (it was an inline `useDropTarget`: every drop moved; now Ctrl/Alt adds, several bookmarks drop at once, the row shows Move / + Add, and a folded row unfolds when a drag rests on it — via the tree's `onOpenChange`); passes `reorderable` to the owned-lists `CollapsibleBookmarkLists` (shared lists aren't reorderable — see below). | Same as above; also re-verify the rows' `useListDrop` wiring after merging. |
| `apps/web/components/dashboard/lists/CollapsibleBookmarkLists.tsx` | Optional `openState` (`{ isOpen(id), setOpen(id, open) }`): the caller keeps which rows are unfolded (the sidebar, for Collapse/Expand all); without it every row keeps its own state, as upstream. Removed the two `.sort((a,b) => a.item.name.localeCompare(...))` alphabetical sorts, replaced with sorting by the new `position` field (descending — newest/most-recently-moved-up sorts first). Added `ReorderableSiblings`, a wrapper that adds HTML5 drag-and-drop reordering (with an insertion-line indicator) around a group of same-parent siblings, gated by a new `reorderable` prop (only the owned-lists tree passes `true` — reordering a shared list's row would silently reorder it for the owner too, since `position` lives on the same DB row regardless of who's viewing). | If upstream changes the sort or the recursion here, keep the `position`-based sort and re-wrap the sibling `.map()` calls (root-level and `ListItem`'s children) in `ReorderableSiblings`. |
| `packages/trpc/models/lists.ts` | Added `List.getNextPosition()` (new lists get `max(siblings) + 1`, so newest sorts first) and `List.reorder()` (moves a list to a new index among its siblings; interpolates a new `position` between its new neighbors — or beyond the top/bottom edge — so only the moved row is touched, no renumbering). `create()` now calls `getNextPosition()`. | Additive — two new methods plus one line in `create()`. Should merge cleanly unless upstream restructures `create()`, in which case keep the `position` assignment. |
| `packages/trpc/routers/lists.ts` | **Backend logic change, not cosmetic.** (1) `stats` rolls a parent (sub)folder's count up from everything nested under it (recursively, via each list's `parentId`), instead of showing only bookmarks added to the parent directly — most "parent" lists are pure organizational folders with 0 bookmarks of their own. (2) Added the `reorder` mutation (owner-only, see `List.reorder()` above). The fork's *other* `stats` change — batching all manual-list counts into one grouped SQL query instead of one query per list — is **no longer a fork change**: upstream landed the same optimisation as `List.getSizes()`, so the hand-rolled version was dropped in favour of theirs and the rollup now runs on top of it. | **The rollup is the part a careless merge silently drops.** If upstream touches `stats` again, read both versions fully — don't take "theirs" by default, since taking theirs wholesale reverts parent folders to showing 0. |
| `packages/trpc/models/assets.ts` | `attachAsset()` now enqueues an `AssetPreprocessingQueue` job (with `assetId` set) whenever a `video` asset is attached, to generate a poster-frame thumbnail. `detachAsset()` now also deletes the orphaned `linkVideoThumbnail` companion asset when its `linkVideo` is detached. | If upstream changes `attachAsset`/`detachAsset`, keep both new blocks (the `if (input.asset.assetType === "video")` enqueue, and the thumbnail-cleanup block in `detachAsset`) and reapply around upstream's version. |
| `apps/workers/workers/assetPreprocessingWorker.ts` | Added `extractAndSaveVideoThumbnail()`: probes the video's duration with `ffprobe` and seeks to **20%** in before grabbing a frame (`-ss` before `-i`, capped to 1280px wide, `-frames:v 1 -update 1`), falling back to a first-frame grab if the probe or seek fails. The 20% seek is the fix for **all-black thumbnails** — a naive first-frame grab captured the black title-card/fade-in that opens most music videos, so those rendered as black boxes in the feed (while the backfill still counted them as "has a thumbnail" since the row existed). `run()` branches early on `req.data.assetId` to target this specific-asset job type before the existing primary-asset logic (unchanged). Its asset-type gate uses the same `linkVideo`-OR-(`bookmarkAsset`-and-video-`contentType`) check as the admin query above (kept in sync manually — the two need slightly different data shapes). | Additive — a new function plus one early branch at the top of `run()`. Should merge cleanly unless upstream restructures `run()`'s dispatch, in which case keep the `req.data.assetId` branch and the new function. |
| `apps/web/components/dashboard/bookmarks/BookmarkVideo.tsx` | Added `thumbnailAssetId` prop: renders the real poster-frame image (via `GatedImage`) behind the play-icon overlay instead of a flat black box; falls back to black if no thumbnail exists yet (older attachments, or extraction failed). | Should merge cleanly — additive prop + conditional render. |

## Non-git-visible change — redo this manually if you ever start a fresh fork

Two of karakeep's own workflows were disabled via the GitHub Actions UI, not
by editing the workflow files — so this won't show up in git history or
survive re-forking from scratch:

- **`CI`** — the full lint/test/typecheck suite. Disabled because Claude runs
  the same checks locally before every push; redundant on a solo fork.
- **`Build and Push Docker`** — upstream's own image-publish workflow. It can
  never succeed on a fork (wrong registry ownership) and just adds noise.

To redo: **Actions → select the workflow (left sidebar) → `⋯` → Disable
workflow.**

## What to test after any upstream merge

- [ ] `pnpm turbo --no-daemon typecheck lint format --continue` — must all pass
- [ ] `pnpm --filter @karakeep/trpc test` — especially `lists.test.ts` (covers the `stats` rewrite)
- [ ] Visually: masonry feed (image + video tiles, hover-dim, white icons), dark/light theme, header/sidebar alignment, the "+" new-bookmark dialog
- [ ] Scroll through a large image- or video-heavy list — confirm no tiles get stuck blank
- [ ] Attach a video and confirm a real poster-frame thumbnail appears in the feed (not a black box) once the worker finishes; check both `ffmpeg` and `ffprobe` are present in the worker's container/environment (the thumbnail seek uses `ffprobe` for duration)
- [ ] "Regenerate all video thumbnails" (Admin → Background Jobs → Asset Preprocessing) replaces existing thumbnails — used to repair the old all-black ones; "Cancel queued jobs" on a queue card aborts an accidental bulk enqueue
- [ ] Settings → Admin → Background Jobs → Asset Preprocessing → "Generate missing video thumbnails" — confirm it enqueues only videos actually missing a thumbnail (not already-thumbnailed ones)
- [ ] Drag a sidebar list to reorder it — confirm the insertion line tracks the cursor and the new order persists after a refresh
- [ ] Open a list with subfolders — confirm its combined count (header + sidebar) equals the sum of its subfolders' counts, and the subfolder tiles at the top of the page link to the right lists
- [ ] `/dashboard/lists` lands on Home; the sidebar's first entry is Home (list-invitation badge, "…" with Sort); a pending invitation shows on Home; on a phone the list icon opens the lists panel and picking a list closes it
- [ ] Preferences follow the account: sign in from a second browser (or as another account) — view options (layout, columns, notes/tags/titles, image fit), theme, language, the tailored feed's lists, every page's sort, sub-list toggles, the sidebar and details toggles are the account's, and another account has its own. A browser that still has pre-update local settings copies them up on its first load where the account has none, then drops the fork's copies (`karakeep-*` in localStorage/cookies)
- [ ] View options: the grid follows the columns slider while it's being dragged, and the choice is there after a reload
- [ ] Tailored feed: "…" → "Choose lists…" (sidebar and page) opens the picker; unticking a folder unticks everything under it and half-ticks its parent; the feed's count and tiles follow
- [ ] Sort: every page's "…" (lists, Tailored feed, Tags, Favourites, Archive, a tag, an RSS feed; the home feed's header button) has Sort. Newest/Oldest/Random (plus Recently added on lists and the feed) stick per page across reloads; Random reshuffles on every visit but not while scrolling, and moving a tile out mid-scroll neither repeats nor skips one
- [ ] Preview: hide the details on one picture, open another — still hidden, until shown again. Scroll over a picture zooms at the pointer, dragging pans, − / + / reset sit bottom right (a click on a disabled one does NOT close the modal); a click on the picture at normal size closes the preview, and the Open button left of the details toggle opens the original. A long title stays on one line; Source, under it, links to where it was saved from
- [ ] Hover a video tile — it plays, muted (Settings → Videos: off, or with sound once you've clicked on the page), and stops when the pointer leaves the card; hover an image tile's magnifier (bottom right) — the picture shows large, moving off hides it, clicking it opens the preview
- [ ] Tags page: typing narrows the cloud, picking two tags shows only what carries both, and the URL keeps the selection; "Manage tags" still reaches the old view
- [ ] A list with sub-lists: "…" → "Show items from sub-lists" brings their items onto the parent's page and survives a reload; dragging one of those onto another list moves it out of the sub-list it was in
- [ ] List icons: no "??" anywhere (sidebar, list picker, browser extension); a new list starts with no emoji and can still be given one
- [ ] Click the header logo — it goes home; the chevron beside it folds/unfolds the sidebar and the state survives a refresh (and shows on another device)
- [ ] Sidebar: Home, Tailored feed, Tags, Favourites on top, then LISTS with its toggle and +; with enough lists unfolded, only the lists scroll, and the rows don't change width when Expand all makes them overflow (check on a Mac with "always show scrollbars"). The toggle reads "Expand all" with everything folded and "Collapse all" once anything is open; the tree stays as you left it after a reload; opening a nested list unfolds its parents, and they can still be folded while it's open
- [ ] Hover a masonry tile — the title, action icons and (on a video) the play mark appear. If nothing appears, Tailwind 3's `group-*`/`peer-*` variants are broken again: upstream's `2d58d906` (cherry-picked here ahead of a merge) pins `tailwindcss@3.4.1>postcss-selector-parser` to 6.1.4 because 6.1.3 silently drops them
- [ ] Open an image, then a video — the modal wraps the media, the details scroll beside it, and a video autoplays exactly once (listen for doubled audio)
- [ ] The preview's details panel (Eagle-style): the name field holds the title/file name and a rename sticks after Enter; Description saves on leaving it; Properties shows the star (toggles), dimensions, size, type and dates; Download saves the original under the bookmark's name; Archive/Delete sit at the bottom under a divider. A picture or video has no Attachments section; a link has one with "+ Add file"/"+ Add banner"
- [ ] In the modal's Lists section: a chip's × removes it; + adds another list; with none left it reads "Unsorted". Drag a tile onto a sidebar list: from a list view it leaves that list, from home it leaves all its lists
- [ ] Drag-and-drop a local `.mp4`/`.webm`/`.mkv` file onto the app — confirm it creates a bookmark (not "Unsupported asset type"), a poster-frame thumbnail appears in the feed within a few seconds without manual intervention, and the video plays in the feed and the preview modal
- [ ] Paste-to-save: take a screenshot snippet (`⌘⌃⇧4` macOS / `Win+Shift+S` Windows), open a manual list, press `⌘/Ctrl+V` — confirm the image is uploaded and added to that list; on the home feed it should upload without a list; and pasting into a text field / note editor should still paste text (not hijack the screenshot)
- [ ] List subscriptions: on a manual list, "…" → "Add subscription", paste a public Pinterest board link. It shows "Syncing…", then "Synced, N new"; the list fills in the board's order, the same picture pinned twice arrives once, and a board with videos brings `.mp4` videos (not their covers). "Sync now" again adds nothing; removing the board and adding it back downloads nothing. Settings → List subscriptions changes the schedule. If a sync fails with HTTP 403, Pinterest changed its front-end — see `connectors/pinterest.ts`
- [ ] Instagram: Settings → List subscriptions → Instagram, paste the `sessionid` cookie → "Connected as @you". On a list, "…" → Add subscription, paste a collection link (`instagram.com/you/saved/<name>/<id>/`) → it syncs pictures, carousel slides (each its own bookmark) and reels (mp4). A second sync takes one or two requests (worker log). Paste a bad session → refused with a message; log out of Instagram in that browser → the next sync fails, Settings says signed out. If a sync reads 0 items, Instagram is sending its web page again: check the `Sec-Fetch-*` headers in `shared-server/src/instagram.ts`
- [ ] Whole carousels: a list's subscriptions have the box on the add form (ticked) and on each subscription. Ticked, a Pinterest carousel pin comes in as all its pictures, each right after the pin, and an idea pin as all its pages; unticked, only the pin's own. Ticking it on a subscription that already synced brings nothing old ("0 new" in the worker log). After the migration, Instagram subscriptions are ticked and Pinterest ones not
- [ ] A list with subscriptions says "↻ Syncs from …" under its name; hovering says when each syncs next (on the hour, per Settings → List subscriptions), clicking opens them, and it reads "Syncing…" while one runs and goes back when it's done
- [ ] Drag and drop (desktop): a picture's card follows the pointer as a small rounded copy of it; drop on a sidebar list → it moves there (from Home: out of every list), "Move" on the row; hold Ctrl (or Alt/⌥) → "+ Add", and it keeps its other lists; rest on a folded list → it unfolds, drop on the sub-list; on a list page, drop on a sub-list tile; Select two or more (card "…" → Select) and drag one → all go, with a count on the drag image; reordering lists in the sidebar still works
- [ ] Phone (iPhone, installed with Safari → Share → Add to Home Screen; it opens on Home, full screen): the tab bar (Home, Feed, Lists sheet, Search, More sheet) on every page, above the home indicator; the view menu's Columns sets the phone's own count (default 2); tap a picture → full-screen viewer: pinch/double-tap zoom, tap hides the bars, drag down closes, drag sideways moves on, Share offers Save Image, Details and Lists open as sheets; long-press a card → its actions, Select → round boxes; New item → Photos picks from the library; More → User Settings is a list, each page has "‹ User Settings"; with the server stopped, the installed app shows "Karakeep can't be reached". Desktop unchanged (80px header, sidebar, preview modal)
- [ ] Duplicate pictures: Cleanups → Duplicate pictures → Check now. The first run downloads the model (worker log: "Downloading the picture model", 352 MB into `DATA_DIR/models`) and looks at every picture; then save a smaller copy of a picture you have and Check now again → "Looked at 1 pictures" and a group of two. Keep this one → the other is gone and its lists/tags/favourite moved over; Not duplicates: keep all → never offered again; the level changes what is shown without a new check. The nightly run: worker log at 03:00 container time. After an upstream merge, check `onnxruntime-node` still loads in the image (a failed check shows its error on the page)
- [ ] Pictures: Settings → Pictures → Fingerprint now → "N of N pictures have one" (the first run downloads the picture model; worker log "[fingerprints] Looked at …"). Cleanups → Check now shows "Waiting for the new pictures' fingerprints…", then the check runs (worker log: fingerprints, then "[duplicates] Compared …")
- [ ] Similar: a picture's details show "Similar" thumbnails; one opens in the preview's place; "See all" opens the grid of them. Settings → Pictures → How similar changes how many
- [ ] Search → Pictures (the mode button in the search bar): "a green sofa" or the like finds the pictures of one; the first search says the text model is getting ready (downloads 254 MB the very first time; worker log "Loaded the text model"); `list:<name>` narrows it; turned off in Settings, the mode is gone. If it stays "getting ready", the `pictureText` worker isn't running
- [ ] List suggestions: with pictures in a few lists, Settings → Pictures → List suggestions → For: All pictures → Cleanups shows suggestions by list; ✓ adds, Add all adds a list's, Not this list removes them for good; a picture's details show "Belongs in" chips. New pictures from a sync get them on their own (worker log: fingerprints, then "[suggestions] Looked at …")
- [ ] Skip near-duplicates: a list subscription with it ticked, syncing pictures you already have as other files, links them ("N near-duplicates of saved ones" in the worker log) instead of downloading them
- [ ] Instagram expired: make the session `expired` (log out of Instagram in that browser and wait for a sync, or set `instagramSessions.status` in the database) → an orange strip above the header on every page but Settings → List subscriptions, the sidebar's Archive still in view at the bottom; ✕ → gone, and still gone after a reload and on another device; paste a fresh session → no strip. A session with `checkedAt` over a day old gets checked on the hour (worker log: "Instagram signed out the session of …" for a dead one)
- [ ] AVIF: drop an `.avif` on a list — it becomes an image bookmark that shows in the grid and the preview, and OCR/AI tagging don't fail on it
- [ ] vrana: the browser tab says "vrana" with the crow; the header shows a small blue square with the crow, then "vrana"; the installed app is the beige crow on blue (on an iPhone, remove it and add it to the Home Screen again: iOS keeps the name and icon it was added with); nothing says Karakeep except the Karakeep export format
- [ ] Theme: in light no surface is white (background, cards, menus and dialogs are beige); dark is warm charcoal with the softer blue; a primary button's label reads well in both
- [ ] Hover a picture tile: after a moment its lists show as chips along the bottom left, clear of the magnifier; a chip opens that list; in four or more lists, two chips and "+N"; drag it to another list, hover again, and the chips have followed
- [ ] Selecting (desktop): hover a picture — a round box top left; click it → the card at the bottom ("1 selected"); click other cards anywhere on them; Shift-click takes everything between; Ctrl-click unpicks; Esc ends it. Hold the mouse on a card half a second → selecting with it (the picture doesn't open). Press on empty space between cards and drag → a blue box picks what it touches (the page scrolls at the edge; Esc during it puts things back). Ctrl+A picks everything loaded. "…" on a list (or the page's row in the sidebar) → Select. Delete → the dialog shows the pictures; Esc closes only the dialog. Drag one of several picked onto a list → the row says "Move 3", the message has Undo, and Undo puts them back where they were
- [ ] Selecting (phone): long press → the actions sheet, Select → the card above the tab bar ("Add to", "Remove from" on a list, 🗑, "…"); the round boxes pass under the tab bar when scrolling
- [ ] Deploy to the NAS and re-test against the real, large dataset before calling it done — several of these bugs only reproduced at real scale (thousands of bookmarks, 100+ lists), not against small local test data
